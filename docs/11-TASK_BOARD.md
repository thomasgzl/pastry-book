# Tableau des tâches

Statuts autorisés : `À faire`, `Prête`, `En cours`, `Bloquée`, `À vérifier`, `Terminée`.

Une tâche passe à `Prête` seulement quand ses dépendances sont `Terminée` et qu'aucun de ses fichiers n'apparaît dans le ledger de verrous ci-dessous.

## Ledger de verrous actifs

Mis à jour par `project-orchestrator` à chaque changement de statut. Vide tant qu'aucune tâche n'est `En cours`.

| Fichiers verrouillés | Agent | Tâche | Depuis |
|---|---|---|---|
| _(aucun — Lot D libéré le 2026-08-14 après validation QA niveau 2 ; D1b restant est une micro-tâche frontend non démarrée)_ | — | — | — |
| _(aucun — Lot E libéré le 2026-08-14 après validation QA niveau 2)_ | — | — | — |
| _(aucun — Lot F-IA libéré le 2026-08-14 après livraison F-IA3/F-IA4/F-IA1/F-IA2, aucun appel réel)_ | — | — | — |
| _(aucun — Lot I clôturé : I4–I10 `Terminée` par preuve de commits, voir réconciliation Lot K)_ | — | — | — |
| _(aucun — Lot J clôturé : J1–J12 `Terminée`, Preview live validée)_ | — | — | — |
| _(aucun — Lot K ouvert le 2026-08-15 : K-S1/K1/K2 `Terminée` (tranche 1 livrée), tranche import K3-DATA/K3-IMPORT/K3-UI et tranche illustrations K5→K12 `Prête`/`À faire` selon dépendances, aucun verrou posé tant qu'elles ne sont pas activées `En cours`)_ | — | — | — |

**Lot F-IA — anti-chevauchement confirmé :** `src/lib/ai/import/**` (F-IA1) et `src/lib/ai/visuals/**` (F-IA2) sont disjoints → vrai parallèle. Fichiers partagés (`.env.example`, garde de coût) écrits **uniquement** par `data-security-agent` (F-IA3/F-IA4), lecture seule pour F-IA1/F-IA2. `data-security-agent` livre F-IA3 + le helper F-IA4 **en premier** (tâche courte), puis F-IA1 et F-IA2 démarrent en parallèle en consommant l'existant. Contrats gelés `src/lib/domain/schemas.ts` / `src/lib/supabase/types.ts` inchangés (le schéma Zod métier est réutilisé tel quel pour les Structured Outputs, jamais modifié).

Réservations à la demande (non encore actives, à inscrire au démarrage de la tâche) :

- `src/components/layout/SiteHeader.tsx` → `frontend-design-agent` pour **D1b** uniquement (branchement du bouton Importer). À ne verrouiller qu'au démarrage de D1b, une fois la route `/importer` créée par D1.

**Contrats gelés, lecture seule pour D et E :** `src/lib/domain/schemas.ts`, `src/lib/supabase/types.ts`, `supabase/migrations/*`. Ils contiennent déjà `import_batches`/`import_items`/`importItemSchema`, `visual_assets`/`visualAssetSchema` (prompt, presetVersion, createdAt, statut `draft`/`approved`/`rejected`, invariant `isPrimary`), et les statuts IA `proposed`/`confirmed`/`rejected`/`needs_review`. Aucun lot ne les modifie. Si D ou E découvre une lacune, il s'arrête et la signale à `project-orchestrator` : `data-security-agent` fera une passe unique consolidée sur le contrat concerné **avant** que D et E ne reprennent, pour éviter tout conflit d'écriture croisé.

**Suivi futur (non bloquant, ouvert le 2026-08-14) :** lacune de contrat mineure sur `src/lib/import/store.ts` — `importItemSchema.sourceFileUrl` est un `z.url()` obligatoire, incompatible avec la saisie manuelle sans fichier. Sans impact tant qu'aucun câblage Supabase réel n'existe. À traiter par `data-security-agent` : rendre ce champ nullable **avant** le prochain lot qui branche réellement Supabase.

## Lot A — Audit

Propriétaire : `project-orchestrator`

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| A1 | Audit du dépôt existant (structure, dépendances déjà installées, git) | — | Terminée | Constat écrit et daté, aucune supposition non vérifiée | `docs/10-AGENT_ARCHITECTURE.md` |
| A2 | Décisions techniques (framework, hébergement, fournisseurs IA) | A1 | Terminée | Décisions validées par l'utilisateur le 2026-08-14, consignées dans `CLAUDE.md` et `docs/10-AGENT_ARCHITECTURE.md`, aucune contradiction avec les principes non négociables, validé par `qa-integration-agent` (mojibake `docs/09-AI_VISUALS.md` corrigé et revérifié) | `CLAUDE.md`, `docs/10-AGENT_ARCHITECTURE.md`, `docs/11-TASK_BOARD.md`, `docs/04-DATA_MODEL.md`, `docs/05-AI_IMPORT.md`, `docs/09-AI_VISUALS.md` |
| A3 | Vérification du mécanisme multi-agents réellement disponible | — | Terminée | `.claude/agents/*.md` créés et conformes au format attendu | `.claude/agents/*.md` |
| A4 | Création des fichiers d'organisation (`10`, `11`, `12`) | A1, A3 | Terminée | Fichiers présents et conformes aux exigences de la mission | `docs/10-AGENT_ARCHITECTURE.md`, `docs/11-TASK_BOARD.md`, `docs/12-INTEGRATION_PROTOCOL.md` |

## Lot B — Fondations techniques

Propriétaire principal : `data-security-agent` ; parallèle après validation des contrats : `frontend-design-agent`

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| B1 | Init Next.js App Router + TypeScript strict + Tailwind + Vitest + Playwright + Zod + client Supabase (déps) | A2 (validée par l'utilisateur) | Terminée | L'application démarre, contrôles de qualité passent, rendu de base testé téléphone/tablette/ordinateur | config racine, `package.json` |
| B2 | Contrats communs (types du domaine, schémas de validation, conventions d'erreur, structure des médias) | B1 | Terminée | Contrats écrits (`src/lib/domain/`), relus et validés par `project-orchestrator`, aucun agent métier n'avait encore commencé | `src/lib/domain/` |
| B3 | Schéma de base de données et migrations (`docs/04-DATA_MODEL.md`) | B2 | Terminée | Toutes les entités du modèle de données créées, invariants respectés (relu manuellement par `project-orchestrator` — validation SQL live impossible, Docker indisponible dans cet environnement) | `supabase/migrations/` |
| B4 | Authentification privée et politiques d'accès | B3 | Terminée | Accès non authentifié bloqué (proxy + RLS + GRANTs explicites), testé unitairement (`route-access.test.ts`) et par requête HTTP réelle (lot C : `middleware.ts` renommé `src/proxy.ts`, seul nom reconnu par Next.js 16, faille corrigée) ; test live contre un vrai projet Supabase à refaire dès qu'un environnement Docker fonctionnel est disponible | `supabase/`, `src/lib/supabase/`, `src/proxy.ts` |
| B5 | Jeu de données de démonstration | B3 | Terminée | Données clairement marquées fictives, couvrant recette minimale et recette détaillée, alias citron, préparations homonymes de sources différentes — validé par `qa-integration-agent` (incohérence mineure de texte corrigée) ; contenu jamais exécuté contre un vrai PostgreSQL (Docker indisponible, même limite que B3/B4) | `supabase/seed.sql` |
| B6 | Tokens du design system mobile-first (couleurs, typographies, espacements, points de rupture à partir du contenu) | B2 | Terminée | Conforme à `docs/06-DESIGN_SYSTEM.md`, contrastes vérifiés (sauge et laiton ancien légèrement assombris pour atteindre 3:1 UI, réservés aux accents/bordures jamais au texte de corps) | `src/app/globals.css` |
| B7 | Composants visuels de base tactiles (carte, bouton, badge « À vérifier ») | B6 | Terminée | États couverts, cibles ≥ 44 × 44 px, clavier accessible, zéro dépendance au survol souris | `src/components/` |
| B8 | Manifeste PWA + service worker + page de repli hors connexion | B7 | Terminée | Installable iOS/iPadOS/Android/ordinateur, mode `standalone`, page déjà chargée consultable hors connexion, mise à jour de version sans double cache indéfini, aucun secret/réponse d'auth mis en cache — validé par `qa-integration-agent` (SW relu ligne à ligne) | `src/app/manifest.ts`, `public/`, service worker, `src/app/offline/` |
| B9 | Harnais de test responsive (téléphone étroit, iPhone récent, tablette portrait, tablette paysage, ordinateur) | B7 | Terminée | Les 5 profils vérifiables à chaque livraison (22 tests passés, 8 skips documentés), zones sûres iOS/iPadOS prises en compte, tests manifeste/SW/hors connexion | `playwright.config.ts`, `tests/e2e/` |
| B10 | Page `/connexion` minimale (nécessaire au middleware B4) | B4, B7 | Terminée | Formulaire accessible, clavier virtuel, états chargement/erreur/succès, aucun secret client, pas d'inscription publique, redirection interne sûre — validé par `qa-integration-agent` | `src/app/connexion/` |

## Lot C — Navigation métier

Propriétaire : `recipe-search-agent` (composition avec composants de `frontend-design-agent`)

Renumérotation 2026-08-14 pour correspondre exactement à la spécification détaillée validée (shell d'abord, coefficient séparé de la fiche recette, recherche globale en dernier).

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| C1 | Structure commune : shell authentifié, en-tête, nav principale/mobile, bouton Importer non fonctionnel, fil d'Ariane, états chargement/erreur/hors connexion/aucun résultat | B4, B7, B8 | Terminée | Hennessy jamais en nav principale, nav tactile mobile cohérente PWA — validé QA (82 tests) | `src/components/`, shell applicatif |
| C2 | Page d'accueil (titre, sous-titre, recherche globale, 4 cartes égales, illustration démo, Importer secondaire) | C1 | Terminée | Aucun widget hors périmètre, 4 cartes poids visuel identique | page d'accueil |
| C3 | Entreprises : `/entreprises`, `/entreprises/:entreprise`, `/entreprises/:entreprise/:categorie` | C1 | Terminée | Hennessy accessible uniquement depuis Entreprises, catégories propres à Hennessy absentes ailleurs, catégorie vide jamais affichée | pages Entreprises |
| C4 | Répertoire des recettes `/recettes` (recherche titre, filtres, état sans résultat, conservation filtres au retour) | C1 | Terminée | Deux recettes homonymes distinguables par source, aucun champ hors périmètre (durée/difficulté/etc.) | page Recettes |
| C5 | Fiche recette adaptative `/recettes/:id` | C3, C4 | Terminée | CAP minimale sans bloc vide, Hennessy détaillée avec informations complémentaires, préparation jamais globalisée — validé QA finale (checklist 15 points) | fiche recette |
| C6 | Coefficient multiplicateur (`× 0,5/1/1,5/2` + personnalisé) | C5 | Terminée | `QS`/absent/`needs_review` jamais inventés, × 1 restitue l'original, calcul pur testé unitairement (17 cas) — validé QA finale | logique + UI coefficient |
| C7 | Matières premières `/matieres-premieres` + fiche matière | C1 | Terminée | Alias citron (jus/zeste/purée) retrouvent Citron sans modifier les libellés source — validé QA | pages Matières premières |
| C8 | Spécificités et allergènes `/specificites`, séparés | C1 | Terminée | `confirmed`/`proposed` visuellement distincts, aucune confirmation par absence d'ingrédient — validé QA | page Spécificités |
| C9 | Recherche globale groupée (Entreprises/Recettes/Matières premières/Catégories) | C3, C4, C7 | Terminée | État vide/erreur, clavier, aucun appel IA, catégorie indique son entreprise parente — validé QA | recherche globale |

## Lot C-bis — Correction visuelle (skin éditorial, aucun changement de comportement)

Ajouté 2026-08-14. Phase intermédiaire demandée par l'utilisateur avant D/E : rendre l'interface fidèle au mood board « Botanique éditorial ». **Périmètre strictement visuel.** Interdiction absolue de toucher : routes, hiérarchie Entreprises→Hennessy→Catégorie→Recette, recherche, filtres, alias ingrédients, données démo, logique coefficient, règles QS/quantité absente/needs_review, sections conditionnelles, statuts confirmed/proposed, PWA, auth. Aucun test métier existant ne doit changer de comportement ; tout test qui casse pour cause de refonte visuelle est corrigé au niveau du sélecteur, jamais de la logique. Aucune génération IA payante.

Propriétaires : `frontend-design-agent` (implémentation), `ai-visuals-agent` (contrat de placeholders uniquement). Validation finale unique : `qa-integration-agent`.

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| CBV1 | Contrat des placeholders visuels + assets SVG temporaires (emblème monogramme, branches botaniques trait olive, cadre culinaire) — AUCUNE génération payante | C5 | Terminée | Contrat écrit (dimensions, ratios, nommage, emplacements où les visuels IA viendront) ; assets clairement marqués « temporaire / remplaçable » ; aucun appel IA — 10 SVG créés (`emblem`, `placeholder-hero`, `placeholder-recipe-4x3`, 6× `botanical-*`, `ornament-branch`), tableau des emplacements dans `docs/09-AI_VISUALS.md` § Contrat des placeholders | `public/visuals/placeholders/`, `docs/09-AI_VISUALS.md` |
| CBF1 | Couche design system : palette renforcée + composants/tokens réutilisables (ornement fin, illustration temporaire, carte d'entrée, cadre visuel culinaire, titre éditorial, section préparation, ligne ingrédient, état « À vérifier ») | CBV1 | Terminée | Zéro style dupliqué par page ; contrastes accessibles ; sauge/laiton jamais sur texte long — validé QA finale | `src/app/globals.css`, `src/components/ui/*`, `src/components/cards/*` |
| CBF2 | Accueil recomposé : emblème temporaire, héro éditorial responsive, recherche plus visible, 4 cartes d'entrée à icônes fines olive, grilles responsive, suppression du 2e bouton Importer | CBF1 | Terminée | Carré « L » remplacé ; 2e Importer supprimé (header conservé) ; grilles mobile 1col / tablette portrait 2×2 / paysage-desktop 4col ; 4 cartes poids visuel égal — validé QA finale | `src/app/(app)/page.tsx` |
| CBF3 | Navigation/shell : libellés et zones cliquables desktop augmentés, onglet actif visible, menu compact mobile cible 44×44, titre non comprimé, Importer accessible ; retrait de tout badge dev (cercle « N ») / bouton flottant non documenté | CBF1 | Terminée | Aucun artefact de dev visible ; onglet actif distinct ; menu mobile lisible ouvert ; Importer accessible aux deux formats — débordement horizontal tablette portrait trouvé en QA puis corrigé (`SiteHeader.tsx`), revérifié | `src/components/layout/SiteHeader.tsx`, `src/app/(app)/layout.tsx` |
| CBF4 | Fiche recette éditoriale : header (titre/source/catégorie/fil d'Ariane réduit mobile), retrait de l'image de désert + visuel remplacé (ratio ~4:3 desktop/paysage, pleine largeur arrondie mobile/portrait, décor botanique léger), coefficient présentation seule, ingrédients plus lisibles | CBF1 | Terminée | Image de désert retirée (`RecipeSheet.tsx` et `RecipeCard.tsx`) ; « Coefficient » libellé visible, raccourcis+perso groupés, état actif clair, texte « les quantités ne changent jamais » conservé, AUCUNE modif logique ; quantités alignées droite chiffres tabulaires, « À vérifier » et « QS » inchangés ; largeur fiche plus étroite — validé QA finale | `src/app/(app)/recettes/[slug]/RecipeSheet.tsx`, `src/components/cards/RecipeCard.tsx` |
| CBF5 | Largeurs et langage botanique transverses : max ~1180-1320px desktop (fiche plus étroite), tablette portrait/paysage optimisées spécifiquement, mobile 1col sans scroll horizontal, décor botanique fin masquable mobile jamais sous texte important | CBF2, CBF3, CBF4 | Terminée | Aucun défilement horizontal sur les 4 profils ; décor jamais sous texte long ; largeurs respectées ; tablette non traitée comme un desktop réduit — validé QA finale | `src/app/globals.css`, pages concernées |

## Lot D — Import assisté

Propriétaire : `ai-import-agent`. UI sur demande explicite : `frontend-design-agent`. Contrats/validation/stockage seulement : `data-security-agent`. Un seul checkpoint QA (niveau 2) en fin de lot.

Refonte 2026-08-14 : remplace l'ancien découpage D1–D4 par le périmètre validé (import manuel fonctionnel d'abord, abstraction IA en mode démonstration ensuite, aucun appel payant). L'ancien D4 (normalisation/allergènes/spécificités) est fusionné dans D3, qui l'inclut explicitement.

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| D1 | Parcours d'import manuel fonctionnel (assistant). Bouton Importer → choisir entreprise/source → choisir ou créer une catégorie locale à cette entreprise → ajouter un ou plusieurs fichiers (image, capture, PDF, DOCX, texte collé) → afficher les informations reconnues → correction manuelle → écran de vérification (original + proposition, champs incertains mis en évidence) → **enregistrement uniquement après confirmation humaine**. | B3, B4, B8 | Terminée | Validé QA checkpoint niveau 2 (2026-08-14). Aucune sauvegarde auto avant validation ; formulaire pleinement utilisable **sans fournisseur IA** (aucune dépendance dure à une clé API) ; catégorie propre à l'entreprise choisie, jamais globalisée ; création contrôlée d'une nouvelle catégorie locale ; import possible depuis fichiers et appareil photo tablette/téléphone ; validation recette par recette ; utilisable clavier virtuel ouvert, tablette portrait et paysage, sans défilement horizontal ; états chargement/erreur/aucun résultat présents | `src/app/(app)/importer/**`, `src/lib/import/**` (hors `schema.ts` géré en D2) |
| D1b | Branchement du bouton « Importer » du shell vers la route `/importer` (retrait de l'état désactivé). **Micro-tâche `frontend-design-agent`**, sur demande explicite de `ai-import-agent`, une fois `/importer` créée. | D1 | Repris dans J2 (fusionnée le 2026-08-14) | Bouton actif aux deux formats (mobile/desktop), cible ≥ 44×44 px, aucune régression du header, aucune modification de logique métier | `src/components/layout/SiteHeader.tsx` |
| D2 | Modèle d'import + validation stricte (Zod, spécifique à l'import, concrétise `proposedRecipe` laissé `unknown` dans le contrat gelé — **ne modifie pas `src/lib/domain/schemas.ts`**). Champs conditionnels : titre, source/entreprise, catégorie locale facultative, préparations, ingrédients, quantité originale, unité originale, procédé/température/informations complémentaires/spécificités/allergènes facultatifs, image d'origine. | D1 | Terminée | Validé QA checkpoint niveau 2 (2026-08-14). Aucune information inventée ; quantité illisible → `null` + `À vérifier` ; `QS` conservé tel quel ; libellé original de l'ingrédient conservé ; ingrédient canonique **proposé séparément**, jamais fusionné dans la donnée source ; préparation attachée à sa recette, jamais globalisée (principe 7) ; aucune section vide affichée ; import idempotent ou détection claire de doublon | `src/lib/import/schema.ts`, `src/lib/import/model.ts`, tests colocalisés |
| D3 | Abstraction serveur IA d'import **indépendante du fournisseur** (port + adaptateur), couvrant OCR, extraction structurée, normalisation des ingrédients, proposition d'allergènes, proposition de spécificités (OpenAI = premier fournisseur cible). **Aucun appel payant : mode démonstration déterministe uniquement.** Ne pas traiter les 600 recettes : créer **3 exemples démo contrôlés** — (a) recette CAP ingrédients seuls, (b) recette Hennessy détaillée, (c) capture avec quantité illisible. | D1, D2 | Terminée | Validé QA checkpoint niveau 2 (2026-08-14). Chaque résultat IA porte un état `proposed`/`confirmed`/`rejected`/`needs_review` — jamais certain automatiquement ; texte local PDF/DOCX d'abord, OCR/vision réservé scans/photos/captures ; l'exemple (c) produit `null` + `À vérifier`, **jamais une valeur inventée** ; règles déterministes d'abord pour allergènes/spécificités, IA pour les ambigus, aucune trace ni contamination déduite ; aucun secret côté client ; le code métier n'appelle jamais le SDK du fournisseur directement | `src/lib/ai/import/**` (port, adaptateur démo, fixtures des 3 exemples) |

## Lot E — Illustrations IA

Propriétaire : `ai-visuals-agent`. UI sur demande explicite : `frontend-design-agent`. Contrats/stockage seulement : `data-security-agent`. Un seul checkpoint QA (niveau 2) en fin de lot.

Refonte 2026-08-14 : remplace l'ancien découpage E1–E3 par le périmètre validé (preset versionné, infrastructure interchangeable, quatre usages + galerie de validation, 5 exemples en mode démonstration, aucune génération en masse ni appel payant sans autorisation explicite).

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| E2 | Preset visuel unique « Botanique éditorial — v1 » (validé QA checkpoint niveau 2, 2026-08-14), **versionné** (constante de données). Direction : illustration culinaire fine, encre olive, aquarelle très légère, fond ivoire/transparent, composition aérée, cohérent Bodoni Moda/Karla/cacao/olive/sauge. Exclusions : aucun texte intégré, aucun logo inventé, aucune personne, pas de photoréalisme, pas de rose/terracotta, pas de végétation excessive. | B6 (design system) | Terminée | Preset stocké une seule fois, identifié par version (`v1`) ; exclusions explicites présentes ; aucune génération, aucun appel IA — 10 tests unitaires | `src/lib/visuals/preset.ts` |
| E1 | Infrastructure : fournisseur d'images **interchangeable** (port + adaptateur), mode démonstration **sans clé**, appel serveur uniquement, **aucun secret côté client**. Stockage via le contrat gelé `visual_assets` : image + prompt + version du preset + date de génération + statut `draft`/`approved`/`rejected`. | E2, B3 (table `visual_assets`) | Terminée | Validé QA checkpoint niveau 2 (2026-08-14). **Ne remplace jamais automatiquement un visuel déjà `approved`** ; consomme le preset E2 ; le code métier n'appelle jamais le SDK du fournisseur directement ; aucune clé consommée en mode démo ; date de génération et prompt persistés — 14 tests unitaires (invariant testé explicitement) | `src/lib/ai/visuals/**`, `src/lib/visuals/provider.ts`, `src/lib/visuals/storage.ts`, tests colocalisés |
| E3 | Quatre types d'illustrations (matière première, recette, entreprise, catégorie d'entreprise) + **galerie de validation**. Fonctions : génération individuelle, régénération d'un brouillon, validation ou rejet, génération **uniquement pour les visuels manquants**, prévention des générations accidentelles en masse (confirmation explicite requise avant tout lot). | E1 | Terminée | Validé QA checkpoint niveau 2 (2026-08-14). Cycle `draft`/`approved`/`rejected` complet et visible ; un visuel `approved` jamais écrasé par régénération ; aucun lot déclenché sans confirmation explicite (phrase nommée obligatoire, tout lot, pas de seuil à contourner) ; galerie utilisable tablette portrait/paysage sans scroll horizontal (`<details>` natif, formulaires `<form action>`, aucun état client hors du lot) ; page `/visuels` rendue et vérifiée (`curl` 200, 20 sujets démo listés) | `src/app/(app)/visuels/**`, logique dans `src/lib/visuals/**` |
| E4 | **5 exemples maximum, mode démonstration UNIQUEMENT** : Citron, Pistache, Tarte au citron, ambiance Hennessy (sans logo reproduit), catégorie « Recettes de base ». Passerelle coût : avant tout appel payant réel, afficher fournisseur, modèle, nombre exact d'images, estimation de coût si disponible, et **attendre l'autorisation explicite de l'utilisateur**. | E2, E3 | Terminée | Validé QA checkpoint niveau 2 (2026-08-14). Sans autorisation utilisateur : aucun appel réel, aucune clé consommée ; les 5 exemples cohérents avec le preset v1 ; ambiance Hennessy sans logo reproduit (`draft`, en attente de validation humaine) ; validation manuelle avant toute publication — 8 tests unitaires + 4 tests Playwright (tablette portrait) | `src/lib/visuals/fixtures/**` (5 exemples démo), `src/lib/visuals/storage.ts` (amorçage), `tests/e2e/visuels.spec.ts` |

## Lot F-IA — Adaptateurs OpenAI réels (code seulement, AUCUN appel payant dans cette tranche)

Ouvert le 2026-08-14. Mode économie de tokens strict (§9). Territoires `import`/`visuals` disjoints (parallèle). Un seul checkpoint QA final. Sans clé présente : chaque adaptateur se dégrade proprement vers le mode démonstration existant, aucune erreur bloquante. Le JSON du modèle est toujours revalidé côté serveur avec le schéma Zod existant. Aucune génération/extraction réelle, aucun build complet, aucun Playwright 5 profils : mocks uniquement.

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| F-IA3 | `.env.example` : ajouter `OPENAI_EXTRACTION_MODEL=gpt-5-mini` et `OPENAI_IMAGE_MODEL=gpt-image-2` (sans valeur réelle ; `OPENAI_API_KEY=` et `DEMO_MODE=true` déjà présents). Écrivain unique pour éviter tout conflit F-IA1/F-IA2. | — | Terminée (Validé, aucun appel réel effectué — 2026-08-14, commit f87dd63) | Aucune vraie clé/valeur committée, aucun préfixe `NEXT_PUBLIC_*`, 2 lignes ajoutées sans écraser l'existant | `.env.example` |
| F-IA4 | Garde de coût partagée (helper pur, neutre) : identifiant d'idempotence, garde anti-relance/min-délai entre deux demandes, compteur d'appels horodaté **sans contenu sensible** (pas de texte recette ni prompt complet). Consommée par les deux adaptateurs. Mécanisme seulement, aucun appel déclenché. | F-IA3 | Terminée (Validé, aucun appel réel effectué — 2026-08-14, commit f87dd63) | Helper pur testé (idempotence + min-délai), journal sans contenu sensible, une seule implémentation (pas de duplication import/visuels) | `src/lib/domain/aiCostGuard.ts` (+ test colocalisé) |
| F-IA1 | Adaptateur OpenAI extraction réel : SDK officiel serveur, Responses API, Structured Outputs contraints par `src/lib/domain/schemas.ts` existant. Modèle via `OPENAI_EXTRACTION_MODEL` (défaut `gpt-5-mini`, const UNIQUE). Entrées texte/DOCX/PDF/image ; texte local prioritaire ; PDF détail `low` par défaut, `high` seulement via action explicite « Document difficile » ; une recette à la fois ; taille/pages plafonnées ; aucun retry silencieux coûteux. Sortie strictement conforme au contrat, revalidée côté serveur ; refus/sortie invalide gérés (pas de crash). | F-IA3, F-IA4 | Terminée (Validé, aucun appel réel effectué — 2026-08-14, commit 80f0308) | JSON modèle **revalidé** Zod ; jamais d'info supposée ; pas de déduction vegan/sans-gluten par absence ; `QS` textuel ; illisible → `null`+`needs_review` ; résultats `proposed` jusqu'à validation humaine ; clé jamais exposée client ; sans clé → repli démo propre. Tests mocks : contrat, sortie invalide/refus, `QS`, quantité illisible, absence d'info inventée, protection de clé, anti-double-clic/idempotence | `src/lib/ai/import/**` |
| F-IA2 | Adaptateur OpenAI illustrations réel : Image API, modèle via `OPENAI_IMAGE_MODEL` (défaut `gpt-image-2`, const UNIQUE), preset « Botanique éditorial — v1 » existant. Génération simple par requête (pas de batch). Deux niveaux : brouillon économique / qualité finale seulement après confirmation humaine explicite. Aucune génération à l'ouverture d'une page ; confirmation avant chaque appel ; nombre exact d'images + fournisseur/modèle affichés ; jamais d'écrasement d'un visuel `approved` (règle `storage.ts` existante respectée) ; prompt+modèle+preset+statut enregistrés ; décodage/stockage serveur uniquement ; aucune clé côté client. | F-IA3, F-IA4 | Terminée (Validé, aucun appel réel effectué — 2026-08-14, commit f673812) | Non-écrasement `approved` testé ; aucun appel auto ; const modèle unique ; sans clé → repli démo propre ; clé jamais exposée client. Tests mocks : contrat, sortie invalide/refus, non-écrasement `approved`, anti-double-clic/idempotence, protection de clé | `src/lib/ai/visuals/**` |

## Lot I — Persistance Supabase réelle + Preview Vercel

Ouvert le 2026-08-14 après incident réel : un visuel « Citron » généré et approuvé via un appel OpenAI réel (lot G) a été perdu au redémarrage du serveur de dev, car aucune écriture n'atterrit dans Supabase. Tous les stores applicatifs sont encore en mémoire process. Objectif du lot : brancher Supabase réellement (base + Storage + RLS) puis déployer une Preview Vercel.

**Constat d'audit I1 (statique, aucun test réel exécuté — Supabase n'est ni validé ni confirmé fonctionnel) :**

- Migrations présentes : `20260814090000_schema.sql` (toutes les tables du modèle, dont `import_batches`/`import_items`/`visual_assets`, triggers, invariant un seul visuel principal) et `20260814090100_rls_policies.sql` (RLS + GRANT `authenticated` sur les 15 tables, jamais `anon`). Ces migrations n'ont jamais été exécutées contre un vrai PostgreSQL (Docker indisponible depuis B3).
- Clients Supabase (`src/lib/supabase/{client,server,admin}.ts`) écrits et **uniquement consommés par `src/proxy.ts` (auth) et `src/app/connexion/page.tsx` (login)**. Aucune lecture ni écriture de données métier ne passe par Supabase aujourd'hui.
- Stores en mémoire à remplacer : `src/lib/visuals/storage.ts` (visual_assets — le visuel perdu), `src/lib/import/store.ts` (batches/items/recettes/catégories de session), `src/lib/demo/data.ts` (source de lecture read-only de toute l'app via `src/lib/data/*`).
- **Lacune de migration bloquante avant câblage** : `import_items.source_file_url` est `not null` dans le schéma SQL, alors que le contrat gelé `schemas.ts` l'a rendu `nullable` (`sourceFileUrl: z.url().nullable().optional()`). La saisie manuelle sans fichier écrirait `null` → violation NOT NULL. À corriger dans une nouvelle migration (I2).
- **Lacune Storage** : aucun bucket configuré (tous commentés dans `config.toml`). Les photos d'import, documents originaux et octets des visuels IA n'ont nulle part où aller. À créer (I2).
- `.env.local` présent localement et **non suivi par Git** (confirmé `git ls-files`) — contient la vraie clé OpenAI ajoutée par l'utilisateur (lot G). Ne jamais le committer ni en lire le contenu.
- `.env.example` en état supprimé (D) dans `git status`, **antérieur à cette session** : ne pas le restaurer ici, I10 le recrée proprement sans valeur réelle.

Propriétaires : `data-security-agent` (migrations, clients, adaptateurs persistants — seul propriétaire des migrations), `ai-import-agent` / `ai-visuals-agent` (upload média + consommation des adaptateurs), `recipe-search-agent` (lecture via Supabase avec repli démo), `qa-integration-agent` (checkpoint niveau 3 : modification transversale contrats/BDD/auth). Actions interactives réservées à l'utilisateur, marquées **BLOQUANT — action humaine requise**.

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| I1 | Audit préalable statique (migrations, stores mémoire, points d'appel, exclusion des secrets, état Git, plan de migration) — **aucun appel réseau, aucun secret écrit** | — | Terminée | Constat écrit et daté ; aucune affirmation que Supabase est fonctionnel ; inventaire vérifié par lecture réelle et grep, pas supposé | `docs/11-TASK_BOARD.md` |
| I2 | Migration corrective **avant tout câblage** : rendre `import_items.source_file_url` nullable ; créer les buckets Storage privés (documents d'import, photos, visuels IA) + politiques d'accès Storage réservées à `authenticated`/`service_role`. Nouvelle migration additive, jamais destructrice. | I1 | À faire | `source_file_url` nullable aligné sur `schemas.ts` ; buckets privés (jamais publics) ; aucun `anon` ; aucune donnée existante détruite ; relu par `project-orchestrator` (validation SQL live impossible sans projet lié — voir I3) | `supabase/migrations/` (nouveau fichier), `supabase/config.toml` (buckets) |
| I3 | **BLOQUANT — action humaine requise** : `supabase login`, `supabase link` au projet cloud, `supabase db push` (migrations + I2), création du compte administrateur, remplissage des variables Supabase dans `.env.local` (URL, anon key, service_role key). Seul l'utilisateur peut le faire. | I2 | Bloquée | Migrations appliquées au projet réel confirmées par l'utilisateur ; secrets **jamais** committés ni partagés en conversation ; à cet instant seulement Supabase peut être dit « connecté » | `.env.local` (hors Git), projet Supabase cloud |
| I4 | Adaptateur persistant `visual_assets` : remplacer l'état mémoire de `src/lib/visuals/storage.ts` par de vraies requêtes Supabase (client serveur/admin), **même signature d'API** pour ne pas toucher les appelants. Invariant non négociable : un visuel `approved` n'est jamais écrasé ; un seul principal par sujet (déjà garanti par l'index unique SQL). | I3 | À faire | API inchangée (appelants `service.ts`/`actions.ts`/pages intacts) ; `approved` jamais écrasé (test conservé) ; lecture/écriture réelles ; aucune clé côté client ; repli démo propre si Supabase absent | `src/lib/visuals/storage.ts` |
| I5 | Adaptateur persistant import : remplacer l'état mémoire de `src/lib/import/store.ts` par écritures réelles dans `import_batches`/`import_items`/`recipes`/`recipe_sections`/`recipe_ingredients`/`source_categories`. Idempotence conservée (re-clic ne crée pas de doublon) ; enregistrement **uniquement après confirmation humaine**. | I3, I4 | À faire | Recette validée persiste après redémarrage ; idempotence réelle (contrainte + relecture) ; catégorie locale jamais globalisée ; `sourceFileUrl` null accepté (dépend de I2) ; API inchangée pour `ImporterWizard.tsx` | `src/lib/import/store.ts` |
| I6 | Upload média vers Storage : photos/documents d'import (`ai-import-agent`) et octets des visuels IA décodés côté serveur (`ai-visuals-agent`) écrits dans les buckets privés de I2, URLs persistées dans les colonnes existantes. Aucun octet ni clé côté client. | I2, I3, I4, I5 | À faire | Fichiers dans buckets privés ; URLs signées/servies via le serveur uniquement ; aucun upload automatique non confirmé ; jamais d'écrasement d'un visuel `approved` | `src/lib/ai/import/**`, `src/lib/ai/visuals/**` |
| I7 | Lecture des données métier via Supabase avec **repli démo** : `src/lib/data/*` interroge Supabase quand `hasSupabaseConfig()` est vrai, retombe sur `src/lib/demo/data.ts` sinon. Aucun changement de signature pour les pages consommatrices. | I3 | À faire | App fonctionne toujours sans Supabase (mode démo) ; avec Supabase, lit les vraies données ; aucune donnée inventée ; alias citron et catégories locales préservés | `src/lib/data/*` |
| I8 | **BLOQUANT — action humaine requise** : connexion Vercel, import du dépôt, saisie des variables d'environnement Vercel (Supabase + OpenAI), déclenchement d'une Preview. Seul l'utilisateur peut connecter Vercel et saisir les secrets. | I4, I5, I6, I7 | Bloquée | Preview déployée et joignable confirmée par l'utilisateur ; secrets saisis côté Vercel uniquement, jamais committés ; auth privée effective sur la Preview | Tableau de bord Vercel, variables d'environnement Vercel |
| I9 | Checkpoint QA **niveau 3** (modification transversale contrats/BDD/auth, §9) sur la Preview : persistance réelle après redémarrage (le visuel Citron survit), auth privée bloque l'accès anonyme (requête HTTP réelle), 5 profils responsive, PWA/hors connexion. | I8 | À faire | Le scénario de l'incident ne se reproduit plus ; route protégée renvoie bien un refus non authentifié (leçon lot C : requête HTTP réelle) ; rapport pass/fail par critère | rapports de validation |
| I10 | Documentation : recréer `.env.example` (toutes les variables requises **sans valeur réelle** : Supabase URL/anon/service_role, OpenAI, DEMO_MODE) et documenter la procédure de connexion Supabase + déploiement Vercel. Purement documentaire. | I2 | À faire | `.env.example` recréé sans aucun secret, aucun préfixe `NEXT_PUBLIC_*` sur une clé privée ; procédure reproductible ; cohérent avec `.gitignore` (`.env*` ignoré sauf `.env.example`) | `.env.example`, doc de déploiement |

**Anti-chevauchement Lot I :** I4 (`src/lib/visuals/storage.ts`) et I5 (`src/lib/import/store.ts`) touchent des fichiers disjoints mais I5 dépend de I4 (adaptateur média/visuel réutilisé) → séquentiel, pas parallèle. I6 réutilise l'aire visuals (après I4) et l'aire import (après I5). I7 (`src/lib/data/*`) est disjoint de I4/I5/I6 → peut avancer en parallèle une fois I3 fait. Les migrations (I2) restent la propriété exclusive de `data-security-agent` (règle anti-chevauchement 3). Aucun verrou n'est posé tant qu'une tâche n'est pas activée `En cours` par `project-orchestrator`.

## Lot J — Finition visuelle complète de la Preview

Ouvert le 2026-08-14 après validation utilisatrice du lot I (persistance Supabase réelle + Preview Vercel `https://pastry-book-koi44a8vy-thomasgzls-projects.vercel.app`). **Périmètre strictement visuel + mécanisme sécurisé d'affichage des visuels ; aucune génération/extraction IA payante, aucune modification de contrat/migration/variable Vercel, aucun changement de logique métier (routes, recherche, filtres, alias, coefficient, règles QS/`À vérifier`/statuts, sections conditionnelles).** Tout test métier qui casse pour cause de refonte visuelle est corrigé au niveau du sélecteur, jamais de la logique (même règle que le lot C-bis).

Propriétaires : `frontend-design-agent` (l'essentiel de l'implémentation visuelle), `data-security-agent` (J5 uniquement — mécanisme sécurisé de lecture Storage : URL signées / téléchargement authentifié côté serveur ; jamais de style visuel), `ai-visuals-agent` (J9 uniquement — préparer le rebranchement des pilotes Citron/Pistache **sans aucun appel réel**), `recipe-search-agent` (câblage de composants d'affichage dans les pages de consultation dont il est propriétaire, phase 2 de J7 uniquement). Validation finale unique : `qa-integration-agent` (J11, niveau 3 sur la Preview).

**Constat d'audit J0 (2026-08-14, statique + captures réelles Preview locale, 5 gabarits, utilisateur de test jetable créé via `service_role` puis supprimé — comptage avant/après : 0 utilisateur `lotj-audit-*` restant). Aucun secret lu ni affiché.** L'interface est déjà globalement fidèle à la direction « Botanique éditorial » (Bodoni Moda titres / Karla interface bien appliqués, palette ivoire/cacao/olive respectée, aucun rose/terracotta/aplat sombre, Hennessy jamais en nav principale — seulement carte Entreprises + fil d'Ariane, catégories vides masquées, aucune section vide en fiche recette, QS/`À vérifier`/statuts `confirmé`/`proposé` distincts, coefficient non destructif). Écarts concrets priorisés à traiter dans ce lot :

- **P1 — Débordement de l'ornement botanique hors du `CulinaryFrame`** (`-bottom-6 -right-6 w-28`) : sur accueil (héro) et fiche recette, le trait olive + points sort du coin bas-droit du cadre, lisible comme un artefact ; sur desktop il approche le bord de fenêtre (risque de défilement horizontal). Le plus visible sur tablette portrait (héro surdimensionné).
- **P1 — Couleur d'erreur détournée** : la légende « Visuel à générer — … » (baked dans `placeholder-hero.svg` / `placeholder-recipe-4x3.svg`) est rendue en brun-rouge `--color-brunrouge`, réservé aux erreurs/`À vérifier`. À passer sur un ton neutre/sauge.
- **P2 — Emblème/monogramme d'accueil** : cercle pointillé + squiggle très « brouillon », peu fidèle à un emblème éditorial. À redessiner (SVG statique, aucun IA).
- **P2 — Héro d'accueil surdimensionné et vide sur tablette portrait** : le cadre occupe ~la moitié de la hauteur utile pour un placeholder vide ; équilibrer densité/hauteur.
- **P2 — `Carte recette` sans cadre visuel** : le design system prévoit un visuel (ratio) ; actuellement seul un monogramme carré `PlaceholderIllustration`. Introduire le cadre visuel (placeholder puis vrai visuel via J5).
- **P2 — Fiche matière première sans illustration botanique en tête** : le design system prévoit l'illustration botanique ; la page détail n'affiche que le titre + liste de recettes.
- **P3 — Nav tablette portrait** : « Matières premières » passe sur deux lignes dans la barre ; ajuster l'échelle/espacement sans casser la cible 44 px.
- **P3 — Bouton « Importer » du header encore `disabled`** alors que `/importer` existe (ex-D1b) ; à activer (fusionné dans J2).
- **P3 — Grandes zones vides verticales** sur pages listes desktop (donnée démo peu volumineuse) : à tempérer par la mise en page, sans inventer de contenu.
- **Hors périmètre J (signalé à `recipe-search-agent`, non bloquant)** : une spécificité à « 0 recettes » (ex. « Sans gluten ») reste listée et mène à un état vide — comportement de consultation, à trancher hors de ce lot visuel.

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| J1 | **Consolidation des tokens du design system** (base de tout le reste) : échelles typographiques Bodoni Moda/Karla, espacements, rayons, ombres, et surtout un **token sémantique de légende/placeholder distinct de `--color-brunrouge`** (erreur). Aucune régression de contraste (sauge/laiton jamais sur texte de corps). | — | Prête | Tokens centralisés, zéro couleur d'erreur détournée pour un label non-erreur ; contrastes ≥ seuils WCAG conservés ; aucun style dupliqué introduit | `src/app/globals.css` |
| J2 | **Header/nav** : activer le bouton « Importer » vers `/importer` (retrait `disabled`, **absorbe D1b**), onglet actif net, échelle des libellés en tablette portrait (« Matières premières » sur une ligne), cohérence titre court/complet. Aucune logique métier touchée. | J1 | À faire | Bouton Importer actif aux deux formats, cible ≥ 44×44 px ; nav tablette portrait sans retour à la ligne parasite ; Hennessy toujours absent de la nav ; aucune régression header | `src/components/layout/SiteHeader.tsx`, `src/app/(app)/layout.tsx` |
| J3 | **Accueil** : emblème/monogramme éditorial définitif (SVG statique, **aucun IA**), équilibrage du héro sur tablette portrait, densité/espacements des 4 cartes (poids visuel strictement égal conservé), recherche mise en valeur. | J1, J4 | À faire | Emblème net et fidèle ; héro tablette portrait équilibré (plus dominant/vide) ; 4 cartes poids égal ; aucun 2e bouton Importer ; aucun widget hors périmètre | `src/app/(app)/page.tsx`, `src/components/ui/EntryCard.tsx`, `public/visuals/placeholders/emblem.svg` |
| J4 | **`CulinaryFrame` + ornement botanique** : corriger le débordement hors cadre (P1) — ornement contenu ou repositionné sans franchir le bord ni provoquer de scroll horizontal ; légende placeholder sur ton neutre (P1) ; préparer l'affichage « vrai visuel approuvé si présent, placeholder sinon » (consomme J5). | J1 | À faire | Aucun trait hors cadre sur les 4 profils ; aucun défilement horizontal induit ; légende non-rouge ; bascule placeholder/visuel réel propre (repli placeholder si Storage absent) | `src/components/ui/CulinaryFrame.tsx`, `src/components/ui/BotanicalOrnament.tsx`, `public/visuals/placeholders/*.svg` |
| J5 | **Mécanisme sécurisé de lecture Storage des visuels** : URL signées / téléchargement authentifié servis **côté serveur uniquement** pour afficher un visuel `approved` sans exposer le bucket privé. Signature d'API pensée pour être consommée par `CulinaryFrame`/cartes/galerie ; repli propre (placeholder) si Supabase/Storage absent. **Aucun style visuel.** | I4, I6 (persistance/upload visuels) | Bloquée (dépend de la clôture réelle de I4/I6) | Aucune URL de bucket privé exposée au client ; URL signées expirantes servies serveur ; aucun octet ni clé côté client ; jamais d'écrasement d'un visuel `approved` ; repli placeholder sans Supabase | `src/lib/visuals/storage.ts` (lecture/signature) ou `src/lib/visuals/url.ts` (nouveau), `src/lib/data/*` (chemin lecture visuel) |
| J6 | **Fiche recette** : afficher le vrai visuel approuvé via `CulinaryFrame` + J5 (sinon placeholder), polir header (titre/source/fil d'Ariane), colonnes de préparations et largeur de lecture. Aucune modif de la logique coefficient / quantités / statuts. | J1, J4, J5 | À faire | Visuel réel si disponible, placeholder sinon ; aucune section vide ; QS/`À vérifier`/statuts inchangés ; largeur de lecture confortable ; aucune régression métier | `src/app/(app)/recettes/[slug]/RecipeSheet.tsx` |
| J7 | **Cartes recette & illustration matière première** : (phase 1, `frontend-design-agent`) la `RecipeCard` porte un cadre visuel (ratio, placeholder puis vrai visuel via J5) au lieu du seul monogramme ; composant d'illustration botanique de tête réutilisable pour la matière première. (phase 2, `recipe-search-agent`) câblage de ce composant dans la page détail matière première (sa territoire). Séquentiel, verrous disjoints. | J1, J4, J5 | À faire | `RecipeCard` avec visuel/placeholder cohérent ; illustration botanique en tête de fiche matière ; libellés source intacts ; aucune donnée inventée ; aucun défilement horizontal | phase 1 : `src/components/cards/RecipeCard.tsx`, `src/components/ui/*` (composant illustration) ; phase 2 : `src/app/(app)/matieres-premieres/[matiere]/page.tsx` |
| J8 | **Galerie `/visuels`** : vignettes « Manquant » plus soignées, affichage des visuels réels `approved` via J5, cohérence typographique/couleur. Présentation uniquement — aucune génération, la passerelle de coût et la logique de statut restent inchangées. | J1, J5 | À faire | Vignettes cohérentes ; visuels réels affichés quand `approved`, placeholder sinon ; passerelle de coût et statuts `draft`/`approved`/`rejected` inchangés ; utilisable tablette sans scroll horizontal | `src/app/(app)/visuels/**` (présentation uniquement) |
| J9 | **Préparer le rebranchement des pilotes Citron/Pistache** (visuels perdus avant la persistance réelle) **SANS aucun appel réel** : câbler les sujets de démonstration pour qu'ils affichent leur visuel dès qu'il sera régénéré + `approved`, état/fixtures cohérents, aucune génération déclenchée. | J5 | À faire | Aucun appel OpenAI, aucune clé consommée ; ne prétend jamais qu'un visuel Citron/Pistache est disponible tant qu'il n'est pas régénéré+approuvé ; câblage prêt à recevoir le vrai visuel via J5 | `src/lib/visuals/fixtures/**`, amorçage `src/lib/visuals/storage.ts` (données seulement, disjoint de J8) |
| J10 | **Passe responsive + accessibilité transverse** : 5 profils, zéro défilement horizontal (revérifie l'ornement J4), focus clavier visible, contrastes, cibles ≥ 44 px, tablette portrait/paysage traitées spécifiquement, états vide/erreur/hors-ligne visuellement cohérents. | J2, J3, J4, J6, J7, J8, J9 | À faire | Aucun défilement horizontal sur les 5 profils ; focus visible partout ; contrastes conformes ; décor jamais sous texte long ; états cohérents | pages et composants concernés (présentation) |
| J11 | **Checkpoint QA niveau 3 sur la Preview Vercel** (finition transversale) : 5 profils responsive, PWA/hors-ligne, palette conforme (aucun rose/terracotta/aplat sombre, erreur = rouge/brun réservé), Hennessy jamais en nav, aucune régression métier (recherche, coefficient, alias, statuts, sections conditionnelles). | J10 | À faire | Rapport pass/fail par critère ; aucune régression métier ; direction visuelle « Botanique éditorial » respectée sur les 5 profils | rapports de validation |

**Anti-chevauchement Lot J :** J1 (`globals.css`) est la fondation et se livre **en premier** ; il est disjoint de J5 (`src/lib/visuals/*`, `src/lib/data/*`) → J5 peut avancer en parallèle de J1 dès que I4/I6 sont réellement clôturés. J4 (`CulinaryFrame`/ornement/SVG placeholders) précède J3/J6/J7/J8 qui consomment le cadre. J8 (`src/app/(app)/visuels/**`, présentation) et J9 (`src/lib/visuals/fixtures/**`, données) touchent des fichiers disjoints → parallèle possible. J7 est séquentiel : phase 1 `frontend-design-agent` (composants) puis phase 2 `recipe-search-agent` (page matière première, sa territoire) — jamais les deux verrous simultanément sur les mêmes fichiers. Les migrations et le contrat gelé ne sont **pas** touchés par le lot J. Aucun verrou n'est posé tant qu'une tâche n'est pas activée `En cours` par `project-orchestrator`.

**Dépendance lot I à réconcilier (signalé, non tranché ici) :** le message d'ouverture du lot J indique que le lot I est validé par l'utilisatrice (Preview live, auth Supabase réelle — confirmée pendant l'audit J0 par une connexion réussie contre le vrai projet Supabase). Or le tableau porte encore I4–I10 en `À faire`/`Bloquée`. Ces statuts doivent être réconciliés à partir des rapports de livraison réels **avant** d'activer J5/J6/J8/J9 (qui dépendent de la persistance/upload des visuels I4/I6). `project-orchestrator` ne fait pas avancer un statut sans rapport de livraison ; cette note documente l'écart plutôt que de le masquer.

## Lot K — Imports réels, suppression des pilotes, génération d'illustrations complète

Ouvert le 2026-08-15 après validation utilisatrice de la Preview (suite Playwright J11/J12 mergée). Objectif : passer des pilotes/démos à des fonctionnalités réelles — lectures métier 100 % Supabase en Preview, suppression de toute notion « pilote », import réel atomique avec détection de doublons, centre `/illustrations` avec génération multi-sujets sous garde de coût. **Le lot s'arrête à une nouvelle Preview sur `develop`. Aucun import réel, aucune extraction OpenAI réelle, aucune génération d'image réelle, aucune suppression incertaine de données, aucun déploiement Production ne sont exécutés dans ce lot** — ils attendent une validation humaine séparée (voir K16).

### Réconciliation de statut (précondition de K)

Les commits de `develop` confirment la livraison réelle des lots I et J (persistance Supabase, upload Storage, lecture métier avec repli démo, finition visuelle, suite Playwright contre Preview réelle) : `3c9b8fa` (I5 adaptateurs persistants), `74d25b4` (I6 upload Storage), `45fbba4`/`f9964da` (I9 + fix Preview), `5f7694d`→`c168366` (J1–J12). Sur cette base, **I4–I10 et J1–J12 sont considérés `Terminée`** (preuve = commits + Preview live confirmée par l'utilisatrice). L'écart signalé au §« Dépendance lot I à réconcilier » est ainsi levé. La persistance/upload des visuels (ex-I4/I6) et la lecture métier via Supabase avec repli démo (ex-I7) existent donc déjà : le lot K les **durcit** (K1 supprime le repli silencieux en Preview) plutôt que de les créer.

### Constat d'inventaire K0 (statique, aucun appel réseau, aucun secret lu)

- **Repli démo dans la couche data** : les cinq fichiers `src/lib/data/{sources,categories,recipes,canonical-ingredients,specificities}.ts` référencent tous `demo/data` / `hasSupabaseConfig`. En Preview avec Supabase configuré, ce repli doit devenir une **erreur claire** si Supabase est indisponible, jamais un retour silencieux à la démo (K1).
- **Routes/fichiers pilotes à retirer de l'UX** (K2) : `src/app/(app)/visuels/pilote/**`, `src/app/(app)/visuels/pilote-lot-h/**`, `src/app/(app)/visuels/pilote-pistache/**`, `src/app/(app)/visuels/pilotStyleKit.ts` (+ `.test.ts`), `src/app/(app)/importer/pilotActions.ts`, `src/app/(app)/importer/steps/PilotExtractionPanel.tsx` (+ `.test.tsx`), et toute référence « pilot » dans `ImporterWizard.tsx`/`page.tsx`/`FilesStep.tsx`. **Briques techniques réutilisables à CONSERVER** (jamais « pilote » côté UX mais code réutilisable) : `src/lib/ai/import/**`, `src/lib/ai/visuals/**`, `src/lib/domain/aiCostGuard.ts`, `src/lib/visuals/**` (preset, provider, storage, url). Les fixtures restent uniquement dans les tests.
- **Aucune route `/illustrations`** n'existe encore : K5 la crée comme centre définitif remplaçant les pages pilotes et complétant `/visuels`.
- **Preset « Botanique éditorial — v1 »** déjà versionné dans `src/lib/visuals/preset.ts` (lot E2) : K7 le **réutilise tel quel**, ne le réécrit pas ; il ne fait qu'expliciter les sous-presets matières/recettes/entreprises/catégories s'ils manquent.
- **`.env.example` supprimé non commité** présent depuis le début de session, **antérieur à ce lot** : ne pas le toucher ni le committer dans K sans investigation séparée. Tout agent qui le rencontre le signale à `project-orchestrator`, ne le restaure pas.

### Contraintes absolues du lot (contractuelles, à faire respecter à chaque tâche)

Travailler sur `develop`. Déployer uniquement en Preview. Ne pas modifier la direction artistique validée. Ne jamais revenir silencieusement au stockage mémoire. Ne jamais afficher de données de démonstration en Preview. Ne jamais déclencher automatiquement un appel OpenAI. Chaque opération payante nécessite une confirmation humaine. Aucune relance automatique après un échec. Aucun brouillon visuel ne devient principal automatiquement. Aucune donnée métier inventée. Ne jamais exposer les sources privées ou les secrets dans les journaux. Ne pas lancer de génération massive pendant le développement ou les tests. Tous les appels OpenAI sont mockés pendant les tests.

Propriétaires : `data-security-agent` (schéma/RLS/Storage/transactions — seul propriétaire des migrations), `recipe-search-agent` (lectures métier `src/lib/data/*`), `frontend-design-agent` (UX : nettoyage pilote, écran de correction, centre `/illustrations`, intégration visuels dans les pages), `ai-import-agent` (import réel, extraction, doublons), `ai-visuals-agent` (génération multi-sujets, file des manquants, approbation/versions, fixtures). Validation indépendante : `qa-integration-agent` (niveau 2 après chaque tranche, niveau 3 en fin de lot). Actions payantes/déploiement Production réservées à l'utilisateur, marquées **BLOQUANT — validation humaine requise**.

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| K-S1 | **Vérification schéma / RLS / buckets Storage** préalable à tout câblage réel. Confirmer l'existence et l'exactitude des buckets privés `recipe-sources` et `visual-assets` (jamais publics), des politiques Storage réservées `authenticated`/`service_role`, de `import_items.source_file_url` nullable, et de la RLS sur toute nouvelle opération (transactions d'import atomiques, écriture `visual_assets`). Migration **additive uniquement** si une lacune est trouvée, jamais destructrice. Livrer les lacunes détectées avant K1/K2. | — | Terminée | Buckets privés confirmés/complétés (déjà créés en I2, `20260814090300_storage_buckets.sql` : `recipe-sources` 8 Mo, `visual-assets` 10 Mo, `public=false`, policies select/insert/update/delete réservées `authenticated`) ; aucun `anon` ; `source_file_url` nullable déjà aligné sur `schemas.ts` (`20260814090200`) ; RLS `authenticated`-only déjà posée sur les 15 tables métier dont `import_batches`/`import_items`/`visual_assets` (`20260814090100`), `service_role` bypass RLS pour les écritures serveur — couvre les futures transactions K3-DATA sans modification ; seule lacune réelle trouvée : aucune colonne pour le hash de fichier source (K4) → comblée par une migration additive dédiée. Typecheck + lint + 151 tests unitaires (`src/lib/domain`, `src/lib/supabase`, `src/lib/import`, `src/lib/visuals`, `src/lib/ai/import`) verts après modification des contrats | `supabase/migrations/20260815100000_import_source_file_hash.sql` (nouveau), `src/lib/supabase/types.ts`, `src/lib/domain/schemas.ts` |
| K1 | **Branchement complet des lectures sur Supabase (K1 spec)**. En Preview avec Supabase configuré, tout provient de Supabase (entreprises, catégories, recettes, préparations, ingrédients, matières premières canoniques + alias, allergènes, spécificités, illustrations approuvées) ; nombres de recettes calculés dynamiquement. Le repli démo ne subsiste que pour tests automatisés explicites et développement local sans Supabase volontairement sélectionné. **Aucun fallback silencieux vers démo en Preview** : si Supabase indisponible, erreur claire et sûre. Aucune fusion réel/démo, aucune donnée démo dans les composants. | K-S1 | Terminée | Les 5 fichiers `src/lib/data/*` lisent Supabase (`hasSupabaseConfig()` vrai) via un nouveau loader `supabaseSource.ts` (11 tables, mapping snake_case→camelCase) ; échec Supabase ⇒ `DataAccessError` explicite, jamais un retour silencieux à `demo/data.ts` (testé : mock Supabase en erreur ⇒ `rejects.toThrow`, voir `sources.test.ts`) ; repli démo strictement conditionné à `!hasSupabaseConfig()` (dev local volontaire ou tests, aucune variable Supabase dans `vitest.config.ts`) ; compteurs de recettes toujours calculés dynamiquement (jamais codés en dur) ; alias citron résolus via `resolveCanonicalIngredientId` (mêmes règles qu'en démo, alias Supabase en paramètre explicite) ; catégories toujours locales à leur source. Toutes les fonctions exportées sont devenues asynchrones (Supabase = I/O réseau, incompressible) — répercuté sur tous les appelants de mon périmètre (8 pages `entreprises`/`recettes`/`matieres-premieres`/`specificites`, `/recettes` scindée en `page.tsx` serveur + `RecettesBrowser.tsx` client car elle appelait la couche data en Client Component) et, pour ne pas casser le typecheck global, sur les fichiers hors périmètre mais consommateurs directs non interdits : `src/lib/import/store.ts`, `src/lib/visuals/subjects.ts` (+ test), `src/lib/ai/import/{demoProvider,openaiProvider,runDemoExtraction}.ts`, `src/app/(app)/visuels/{page,actions}.tsx` — changements strictement mécaniques (ajout `async`/`await`), aucune logique modifiée. Typecheck + lint + suite complète (440 tests, 64 fichiers) verts. **Lacune connue, hors périmètre K1 (fichiers `importer/**` explicitement interdits à cette tâche)** : `ImporterWizard.tsx`, `steps/SourceStep.tsx`, `steps/RecipeDetailsStep.tsx`, `steps/ReviewStep.tsx` appellent encore `getSources`/`getCanonicalIngredients`/`getAllergens`/`getSpecificities` de façon synchrone → 4 fichiers en erreur de typecheck (`Property 'find'/'map'/'filter' does not exist on type 'Promise<...>'`) et cassés à l'exécution si rendus (le parcours `/importer` n'est pas fonctionnel pour un import réel dans ce lot, K16 le confirme). **Lacune comblée hors tranche** (build cassé sur `develop`, corrigé avant le checkpoint niveau 2 pour ne pas bloquer K2/tranche 2/3), **en deux passes** : 1) rendre `ImporterWizard`/`SourceStep`/`RecipeDetailsStep`/`ReviewStep` async-safe (props au lieu d'appels synchrones) — insuffisant seul : `qa-integration-agent` a détecté que `next build` échouait quand même (`src/lib/supabase/server.ts` importe `next/headers`, interdit depuis un Client Component même via un appel `async`) ; 2) frontière Server Action ajoutée dans `importActions.ts` (`getImportReferenceDataAction`, `runDemoExtractionAction`) suivant le patron déjà en place (`createImportBatchAction` et consorts) — `ImporterWizard` et `FilesStep` appellent désormais ces actions au lieu d'importer `src/lib/data/*`/`runDemoExtraction` directement. `next build` (Turbopack) vert, typecheck + lint + 440 tests verts. | `src/lib/data/{sources,categories,recipes,canonical-ingredients,specificities}.ts`, `src/lib/data/supabaseSource.ts` (nouveau, loaders Supabase), tests colocalisés ; répercussions mécaniques : 8 pages de consultation, `src/app/(app)/recettes/RecettesBrowser.tsx` (nouveau), `src/lib/import/store.ts`, `src/lib/visuals/subjects.ts`, `src/lib/ai/import/*.ts`, `src/app/(app)/visuels/{page,actions}.tsx`, `src/app/(app)/importer/ImporterWizard.tsx`, `src/app/(app)/importer/steps/{SourceStep,RecipeDetailsStep,ReviewStep,FilesStep}.tsx`, `src/app/(app)/importer/importActions.ts` |
| K2 | **Nettoyage « pilote » (K2 spec)**. Retirer de l'UX toutes routes/liens/titres/boutons/textes « Pilote » et supprimer/rediriger proprement `/visuels/pilote`, `/visuels/pilote-lot-h`, `/visuels/pilote-pistache` et toute route expérimentale vers la fonctionnalité définitive. Retirer `pilotActions.ts`, `PilotExtractionPanel`, `pilotStyleKit`, et les références « pilot » de `ImporterWizard`/`FilesStep`/`page.tsx`. **Conserver les briques techniques réutilisables** (`src/lib/ai/**`, `aiCostGuard.ts`, `src/lib/visuals/**`, tests/fixtures isolés). Ne supprimer AUCUNE donnée Supabase sans inventaire formel : ne jamais supprimer une recette/illustration réelle ni un visuel `approved` sans confirmation ; suppression de fichiers Storage uniquement via l'API Storage. **Si l'identification n'est pas certaine, ne rien supprimer et présenter la liste pour validation** (BLOQUANT). | K-S1 | Terminée | Aucune route/composant pilote accessible ni dans la nav (`next build` confirme la disparition de `/visuels/pilote`, `/visuels/pilote-lot-h`, `/visuels/pilote-pistache` de la table des routes, 404 propre, aucune route de repli publiée puisqu'aucune ne pointait vers ces pages) ; aucune donnée réelle supprimée (aucune suppression Supabase effectuée — voir constat ci-dessous) ; briques techniques intactes (`src/lib/ai/**`, `aiCostGuard.ts`, `src/lib/visuals/**` non touchées) ; fixtures uniquement dans les tests ; typecheck + lint + 43 tests unitaires ciblés (`importer`, `visuels`, `lib/import`) verts, build de production vert. **Constat Supabase (non résolu, signalé sans suppression)** : le code pilote supprimé (`pilote/actions.ts`, `pilote-pistache/actions.ts`, `pilote-lot-h/actions.ts`) a pu écrire de vrais brouillons `visual_assets` pour les sujets réels Citron/Pistache/sujets lot H (`requestId` préfixés `pilot-visual-citron-*`, `pilot-visual-pistache-*`, `pilot-lot-h-*`) une fois I4/I6 en place — ce ne sont pas des données de démonstration (sujets réels), donc aucune suppression n'est justifiée ; aucune requête live n'a été exécutée (`.env.local` jamais lu, règle absolue) ; à vérifier par `data-security-agent`/`ai-visuals-agent` via le futur centre `/illustrations` (K5/K11) au moment de l'approbation normale, pas par suppression. | `src/app/(app)/visuels/pilote*/**` (supprimés), `src/app/(app)/visuels/pilotStyleKit.*` (supprimés), `src/app/(app)/importer/pilotActions.ts` (supprimé), `src/app/(app)/importer/steps/PilotExtractionPanel.*` (supprimés), `src/app/(app)/importer/ImporterWizard.tsx`, `src/app/(app)/importer/steps/FilesStep.tsx`, `src/app/(app)/importer/page.tsx` (refs pilote retirées), commentaires dangling nettoyés dans `src/lib/import/store.ts`, `src/lib/import/imageOptimize.ts`, `src/app/(app)/importer/importActions.ts`, `src/lib/ai/visuals/openai-provider.ts` |
| K3-DATA | **Transactions d'import atomiques + Storage (K3 étape 7, K4)**. Enregistrement atomique uniquement sur clic explicite : une transaction cohérente crée recette + préparations + ingrédients + références fichiers sources + informations complémentaires + propositions + relations canoniques validées. Échec → aucune demi-recette, message lisible, reprise possible, nettoyage sûr des fichiers orphelins. Upload direct vers bucket privé `recipe-sources` (max 8 Mo/fichier, MIME réel validé, aucun base64 en Server Action, aucun accès anonyme, aucun faux `sourceFileUrl`). Détection de doublons sans décision automatique (K4 : même titre+entreprise, même titre autre entreprise, hash fichier identique, préparation homonyme). | K-S1 | Terminée | Atomicité réelle obtenue via une fonction Postgres unique `save_import_recipe` (`supabase/migrations/20260815110000_save_import_recipe_rpc.sql`, exécutable par `service_role` uniquement) appelée par `client.rpc(...)` depuis `saveImportRecipeSupabase` — l'API REST Supabase ne peut pas faire tenir plusieurs écritures multi-tables dans une seule transaction ACID, un corps PL/pgSQL si (toute exception annule tout : recette + préparations + ingrédients + allergènes + spécificités + suivi d'import) ; l'ancien code de compensation JS (suppression de la recette après échec partiel) est supprimé. Idempotence par `draft.id` vérifiée À L'INTÉRIEUR de la fonction (ferme la fenêtre de compétition qui existait entre un aller-retour de vérification séparé et l'insertion). Hash SHA-256 du fichier source calculé côté serveur (`computeSha256Hex`/`fetchAndHashSourceFile`, téléchargement depuis Storage) et persisté dans `import_items.source_file_hash` (colonne K-S1) à chaque enregistrement avec fichier réel. Fichier Storage potentiellement orphelin après échec : **jamais supprimé automatiquement** (une nouvelle tentative avec le même brouillon réutilise le même fichier, une suppression immédiate casserait la reprise déjà gérée côté UI) — averti explicitement par `logPossiblyOrphanedSourceFile` (journal structuré, jamais silencieux) pour vérification/traitement différé. Upload (`sourceUpload.ts`) durci : `validateSourceFile`/`sniffMimeType` rejettent avant tout envoi réseau un fichier > 8 Mo ou dont le contenu réel (signature binaire des premiers octets, jamais l'extension ni `file.type` déclaré par l'appelant) ne correspond à aucun type accepté ; `contentType` envoyé à Supabase = type réellement détecté. Accès anonyme déjà bloqué (RLS Storage `authenticated`-only + `src/proxy.ts` en amont), aucun changement requis, vérifié et documenté. Aucun base64 en Server Action (upload navigateur → Storage direct, inchangé). K4 étendu au-delà du cas déjà branché à l'écran de vérification (même titre + même source, `checkDuplicate` inchangé) via une nouvelle fonction `checkImportDuplicates` (mémoire + Supabase) couvrant les 3 autres cas — même titre dans une AUTRE source, hash de fichier identique, préparation homonyme — jamais bloquante, jamais de fusion automatique, exposée pour que K3-IMPORT/K3-UI la branche dans l'écran de vérification ; non câblée dans `saveImportRecipe` lui-même pour ne pas modifier le texte/comportement de blocage déjà validé et pour ne pas empiéter sur le territoire UI de K3-IMPORT. `ImporterWizard.tsx`/`importActions.ts`/`ReviewStep.tsx` non touchés (signature de `saveImportRecipe`/`checkDuplicate` inchangée). Typecheck + lint + build (`next build`) + 479 tests unitaires verts (dont 3 nouveaux fichiers de tests ciblés : rollback/atomicité via client Supabase mocké, hash SHA-256 avec vecteur connu, `checkImportDuplicates` mémoire, `sniffMimeType`/`validateSourceFile`) ; aucune connexion Supabase live utilisée (mocks uniquement, conforme à la contrainte du lot) | `src/lib/import/store.ts`, `src/lib/import/sourceUpload.ts`, `supabase/migrations/20260815110000_save_import_recipe_rpc.sql` (nouveau), tests colocalisés (`store.test.ts`, `store.rpc.test.ts` nouveau, `sourceUpload.test.ts`) |
| K3-IMPORT | **Parcours `/importer` réel (K3 étapes 1–6, K4)**. Étape 1 destination (entreprise/catégorie locale existante ou création explicite ; jamais proposer les catégories Hennessy chez une autre source). Étape 2 fichiers/texte (image, capture, plusieurs images ordonnées d'une même recette, PDF, DOCX, texte collé ; aperçu, réorganisation, retrait de page). Étape 3 confirmation IA (opération, fournisseur, modèle, nb fichiers, taille, cible, coût estimé ou mention « indisponible », nb d'appels ; clic explicite ; groupe de pages = un seul appel logique). Étape 4 extraction structurée (recette principale vs préparations vs ingrédients/procédés propres à chaque préparation ; **exemple obligatoire « Riz au lait Vanille Caramel » = UNE recette contenant Riz au lait / Caramel / Opaline / Sorbet au lait, jamais quatre recettes**). Étape 6 synthèse de vérification bloquant l'enregistrement silencieux si extraction incomplète (confirmation humaine explicite d'un document réellement incomplet possible). **Aucune extraction OpenAI réelle déclenchée dans ce lot.** | K3-DATA | Terminée | Correctif prioritaire livré : `FilesStep.tsx` importe désormais `MAX_SOURCE_FILE_SIZE_BYTES` (8 Mo, `src/lib/import/sourceUpload.ts`) au lieu d'une constante locale à 20 Mo — message d'erreur client cohérent avec la vraie limite du bucket, testé (`FilesStep.test.tsx`, fichier tout juste sous/au-dessus de 8 Mo). Étape 1 : `getCategoriesAction`/`getCategoriesForSourceIncludingSession` filtrent déjà strictement par `sourceId` (mémoire ET Supabase) — confirmé, catégories Hennessy structurellement invisibles ailleurs. Étape 2 : aperçu ajouté (miniature image via URL objet navigateur, jamais persistée ; étiquette PDF/DOCX sinon), réorganisation monter/descendre + retrait déjà présent, testés (`FilesStep.test.tsx`, 4 tests) ; upload direct navigateur → Storage inchangé, aucun base64 en Server Action (confirmé, aucune modification de `sourceUpload.ts`/`store.ts` au-delà de l'import de la constante). Étape 3 : aucun nouveau point d'entrée d'extraction réelle ajouté — seuls les 3 boutons « exemple de démonstration » (`fixtures.ts`) restent, aucun appel réseau. Étape 4 : comportement multi-préparations déjà correct et déjà couvert par un test dédié existant, non ajouté par cette tâche mais vérifié passant — `rizAuLait.fixture.test.ts` (`buildImportDraft`, chemin partagé démo/réel) confirme UNE recette à 4 préparations (Riz au lait/Caramel/Opaline/Sorbet au lait), ordre conservé, ingrédients/procédés jamais fusionnés entre préparations, extraction partielle jamais acceptée comme `complete`. K4 : nouvelle action `checkImportDuplicatesAction` (`importActions.ts`) branchée dans `ImporterWizard.handleGoToReview`/`checkForDuplicate` (même déclencheur que `checkDuplicateAction` existant, appel non bloquant, échec affiché explicitement jamais silencieux) ; `ReviewStep` affiche désormais un bloc « Différences trouvées » listant chaque cas (`same_title_other_source`/`same_file_hash`/`homonymous_preparation`) sans jamais bloquer l'enregistrement ni fusionner — testé (`ReviewStep.test.tsx`, 3 tests). Étape 5/6 : écran de correction (`RecipeDetailsStep`) et synthèse (`ReviewStep`/`completeness.ts`) non modifiés au-delà de l'ajout K4 ci-dessus — confirmés couvrir déjà destination/titre/préparations/ingrédients/champs à vérifier/doublon/propositions non confirmées/sections manquantes ; `deriveQuantity` (`model.ts`) confirmé : QS → `proposed` jamais converti, illisible/absent → `null` + `needs_review`, jamais une valeur inventée. Étape 7 : écran de succès complété d'un lien direct « Voir la fiche recette » (`/recettes/{slug}`) en plus des liens déjà présents — la recette est immédiatement consultable dans sa fiche réelle, dans `/recettes`, et dans son entreprise/catégorie (lecture Supabase K1, compteurs dynamiques, aucune modification nécessaire côté lecture). Typecheck + lint + `next build` verts ; 490 tests unitaires verts (dont 7 nouveaux : 4 `FilesStep.test.tsx`, 3 `ReviewStep.test.tsx`). Aucune extraction OpenAI réelle déclenchée, aucune fusion automatique de doublon, aucune donnée démo mêlée aux vraies données (fixtures restent dans `src/lib/ai/import/fixtures.ts` et les tests). K3-UI débloquée. | `src/app/(app)/importer/steps/FilesStep.tsx`, `src/app/(app)/importer/steps/FilesStep.test.tsx` (nouveau), `src/app/(app)/importer/steps/ReviewStep.tsx`, `src/app/(app)/importer/steps/ReviewStep.test.tsx` (nouveau), `src/app/(app)/importer/ImporterWizard.tsx`, `src/app/(app)/importer/importActions.ts` |
| K3-UI | **Écran de correction humaine (K3 étape 5)**. Écran complet permettant, avant enregistrement, de modifier : titre recette, entreprise, catégorie locale, ordre des préparations, titre de chaque préparation, ingrédients, quantités, unités, procédés, informations complémentaires, propositions matières premières/allergènes/spécificités. Mobile-first, clavier virtuel ouvert, tablette portrait/paysage, sans défilement horizontal. Aucune matière première canonique/allergène/spécificité confirmé sans validation ; aucune spécificité déduite d'une simple absence d'ingrédient. | K3-IMPORT | Terminée | Vérification complète de `RecipeDetailsStep.tsx` (déjà livré par K3-IMPORT, hors périmètre modifié à tort) : titre/procédé/température/informations complémentaires/ingrédients (libellé original, quantité, unité, canonique)/spécificités/allergènes déjà tous éditables, chacun avec confirmation humaine explicite séparée (bouton « Confirmer », jamais automatique) ; `deriveQuantity` (`model.ts`) confirmé consommé sur chaque frappe de quantité — `QS` jamais converti (`proposed`), illisible/vide → `null` + `needs_review`, jamais une valeur inventée ; aucune ligne du composant ne déduit une spécificité de la simple absence d'un ingrédient (spécificités uniquement ajoutées par choix explicite ou proposition IA déjà présente dans le brouillon). Seule lacune réelle trouvée : l'ordre des préparations n'était pas modifiable (ajout/retrait seulement) — comblé par `moveSection`/deux boutons ↑/↓ par préparation, même patron que `moveFile` (`FilesStep.tsx`, K3-IMPORT), masqués s'il n'y a qu'une seule préparation, désactivés en butée, testés (`RecipeDetailsStep.test.tsx`, 3 tests : aucun bouton à 1 préparation, échange correct, désactivation aux bornes). Titre de chaque préparation déjà éditable (confirmé, aucun changement nécessaire). Entreprise/catégorie locale : confirmées non éditables directement dans cet écran (fixées aux étapes 1/2 du wizard) mais modifiables en revenant en arrière via le bouton « Précédent » toujours visible dans `ImporterWizard.tsx` — cohérent avec le flux en étapes, aucune modification apportée, aucun signalement bloquant. Risque de défilement horizontal en tablette portrait vérifié sur la grille d'ingrédient (`sm:grid-cols-[2fr_1fr_1fr_2fr_auto]`, iPad portrait 810px) : les pistes `fr` ne débordent pas par construction, seul un `<select>`/`<input>` au contenu très long aurait pu forcer un débordement (min-content) ; `min-w-0` ajouté à `inputClasses` (input + select partagent cette classe) en prévention, aucun changement de breakpoint nécessaire (calcul détaillé : ~574px disponibles pour les 6fr après paddings/gaps à 810px, largeurs de colonnes ~95-191px suffisantes pour les libellés démo/réels courts). Typecheck + lint + tests ciblés verts (10/10 sur les 3 fichiers du périmètre : `RecipeDetailsStep.test.tsx` nouveau, `FilesStep.test.tsx`, `ReviewStep.test.tsx`) | `src/app/(app)/importer/steps/RecipeDetailsStep.tsx`, `src/app/(app)/importer/steps/RecipeDetailsStep.test.tsx` (nouveau) |
| K5 | **Centre définitif des illustrations `/illustrations` (K5)**. Espace propre remplaçant les pages pilotes : consulter visuels approuvés / brouillons à vérifier / rejetés / sujets sans illustration / historique des versions. Filtres : recettes, matières premières, entreprises, catégories, statut, recherche par nom. Secondaire (pas un tableau de bord), aucune donnée technique inutile. | K2 | Terminée | Route `/illustrations` rendue (`next build` : entrée `ƒ /illustrations` confirmée), Server Component qui lit `getAllVisualSubjects`/`listVisualAssets` (mêmes sources que `/visuels`, K1) ; **CONSULTATION UNIQUE** — aucune action (approuver/rejeter/régénérer/générer) : ces mutations restent dans `/visuels` (E3) jusqu'à leur intégration par K8-K12, un lien explicite renvoie vers `/visuels?manquants=1` pour les sujets sans illustration (honnête : pas de bouton qui prétend agir sans effet) ; cinq états consultables via un statut représentatif par sujet (`approved`/`draft`/`rejected`/`missing`, `bestVisualStatus` extrait dans `src/lib/visuals/status.ts` et réutilisé par `/visuels/SubjectGallery.tsx`, zéro duplication de logique) + historique complet des versions par sujet dans un `<details>` natif (aucun `<details>` vide : un sujet sans version s'affiche en bloc statique, pas un dépliant sur du vide) ; filtres type de sujet (recette/matière première/entreprise/catégorie via `VISUAL_KIND_LABELS`) + statut + recherche par nom, tous portés par l'URL (`?type=&statut=&q=`, même patron que `RecettesBrowser`/K1, jamais un état perdu à la navigation) ; aucune donnée technique affichée (aucun identifiant Supabase visible en texte, seuls preset/date/statut) ; états chargement (`loading.tsx` natif Next.js, `LoadingState`)/vide (`EmptyState`)/erreur (`ErrorState`, try/catch autour de la lecture) couverts ; aucun appel OpenAI, aucune génération réelle ; extraction de `VISUAL_KIND_LABELS` dans un nouveau module `kindLabels.ts` **sans dépendance `src/lib/data/*`** (bug réel trouvé et corrigé en cours de tâche : `next build` échouait sinon, `IllustrationsBrowser.tsx` — Client Component — important transitivement `next/headers` via `subjects.ts`, même classe de bug que celui documenté en K1 pour `importer/**`) ; `next build` vert, typecheck + lint + 53 tests ciblés `src/lib/visuals/**` + `/visuels` + `/illustrations` verts (dont 16 nouveaux : 12 `page.test.tsx` + 4 `status.test.ts`). **K7 non traité par cette tâche** (temps insuffisant, signalé pour la suite) : `src/lib/visuals/preset.ts` a déjà une structure `FRAMING` par `VisualSubjectKind` qui explicite en grande partie les sous-presets matière première/recette demandés (instruction dédiée, ratio, fond) — à vérifier/compléter par le prochain agent plutôt qu'à réécrire | `src/app/(app)/illustrations/page.tsx`, `src/app/(app)/illustrations/IllustrationsBrowser.tsx`, `src/app/(app)/illustrations/IllustrationEntryCard.tsx`, `src/app/(app)/illustrations/loading.tsx`, `src/app/(app)/illustrations/page.test.tsx`, `src/lib/visuals/status.ts` (+ test), `src/lib/visuals/kindLabels.ts` (nouveau), `src/components/ui/VisualStatusPill.tsx` (nouveau) ; réutilisés sans modification de comportement : `src/app/(app)/visuels/SubjectGallery.tsx` (dédup `bestVisualStatus`), `src/lib/visuals/subjects.ts` (ré-export `VISUAL_KIND_LABELS`) |
| K6 | **Sujets compatibles + déduplication (K6)**. Matières premières canoniques = priorité principale, une illustration unique par ingrédient canonique ; « jus/zeste/purée de citron » → sujet canonique « Citron », libellés originaux conservés sur les fiches ; dédupliquer avant génération ; ne générer que pour les ingrédients réellement présents ou explicitement créés. Recettes : prompt à partir du titre + préparations réellement présentes + ingrédients clés validés + informations complémentaires réelles, sans inventer d'apparence. Entreprises : ambiance générique éditoriale (aucun logo/marque/bouteille identifiable). Catégories : illustration symbolique sans texte. Allergènes/spécificités : pictogrammes du système graphique local en priorité, pas de génération IA payante par item sans décision séparée. | K1 | Terminée | Un seul sujet par ingrédient canonique (alias dédupliqués, test dédié) ; libellés source intacts ; aucune apparence inventée ; aucun logo/marque ; sujets calculés depuis les données réelles — `getAllVisualSubjects()` dérive les sujets `ingredient` **directement depuis le registre canonique** (`getCanonicalIngredients()`), jamais en scannant les alias/libellés recette : les alias (`ingredient_aliases`) ne sont donc structurellement jamais des sujets séparés ; un identifiant stable (UUID de l'entité, déjà conforme à `visualAssetSchema.subjectId`) plus un filet de dédup explicite par id (`dedupeById`) avant tout usage par K8 ; sujets `recipe` enrichis (additifs, rétrocompatibles) de `preparationNames` (titres de section réellement présents), `validatedKeyIngredientNames` (matières canoniques dont au moins un ingrédient source est `verificationStatus: "confirmed"` — `proposed`/`needs_review` explicitement exclus, jamais utilisés pour orienter l'apparence) et `additionalInformation`, pour que K9 construise un prompt sans jamais inventer d'apparence ; sujets `source`/`sourceCategory` inchangés (déjà génériques/symboliques, sans logo, hérités E3). Allergènes/spécificités : non inclus dans `VisualSubjectKind` (`ingredient`\|`recipe`\|`source`\|`sourceCategory`), confirmé hors périmètre de génération IA par défaut — pictogrammes du système graphique local, décision séparée. Tests niveau 1 : 4/4 verts dans `subjects.test.ts` dont 2 nouveaux dédiés (dédup alias jus/zeste/purée de citron → un seul sujet « Citron » + aucun ingrédient non confirmé n'oriente `validatedKeyIngredientNames`) ; typecheck + lint du périmètre verts | `src/lib/visuals/subjects.ts` (dérivation sujets), `src/lib/data/canonical-ingredients.ts` (lecture, déjà K1) |
| K7 | **Presets visuels (K7)**. Réutiliser « Botanique éditorial — v1 » (`src/lib/visuals/preset.ts`, existant) ; expliciter si absents les sous-presets matières premières (sujet botanique isolé, échelle petite carte) et recettes (illustration pâtissière éditoriale d'après infos présentes, jamais photo contractuelle). Exclusions maintenues : aucun texte/logo/personne/main/photoréalisme/rose/terracotta/décor excessif/cadre dessiné. | K5 | Terminée | `VISUAL_PRESET_VERSION`/`BASE_PROMPT` inchangés (aucune nouvelle version, style de référence Citron/G3 non altéré). Écart réel trouvé après vérification (non une supposition) : `FRAMING.ingredient` ne mentionnait ni l'échelle petite carte, ni la tolérance d'éléments secondaires aidant à la reconnaissance, ni l'interdiction explicite d'inventer un ingrédient voisin ; `FRAMING.recipe` ne disait nulle part que l'illustration n'est jamais une photo contractuelle du dessert réel, et n'exploitait pas encore `preparationNames`/`validatedKeyIngredientNames`/`additionalInformation` exposés par K6 sur `VisualSubject` — comblé en étendant (additif) `FRAMING.ingredient`/`FRAMING.recipe` et `BuildVisualPromptInput`/`buildVisualPrompt` (3 champs optionnels recette, rétrocompatibles, silencieux si absents — aucune donnée inventée si l'info manque). `PRESET_EXCLUSIONS` complété (additif) : main, marque/packaging, vaisselle inutile, ombre lourde, cadre dessiné manquaient par rapport à la liste contractuelle (le cadre était déjà dans `docs/09-AI_VISUALS.md` mais pas reflété dans le tableau `preset.ts`) ; aucune nouvelle direction artistique, uniquement des exclusions déjà implicites rendues explicites/vérifiables. `service.ts`/`real-generation.ts` (appelants actuels de `buildVisualPrompt`) non modifiés : ils ne disposent pas encore d'un `VisualSubject` complet à cet endroit — brancher les 3 nouveaux champs optionnels revient à K8/K9 (file des manquants/confirmation), qui manipuleront directement `VisualSubject`. 6 tests ciblés ajoutés (`preset.test.ts`, 15/15 verts) ; typecheck + lint + 74 tests `src/lib/visuals` + `src/lib/ai/visuals` verts ; aucune génération déclenchée. | `src/lib/visuals/preset.ts`, `src/lib/visuals/preset.test.ts` |
| K8 | **File des illustrations manquantes (K8)**. Vue calculant dynamiquement les sujets sans visuel approuvé (ingrédients canoniques, recettes, entreprises, catégories). Afficher avant génération : total, regroupement par type, sujets sélectionnés, modèle, qualité, dimensions, nb exact d'appels, estimation coût si dispo. Sélection individuelle/par type/désélection/recherche/génération d'un seul sujet/lots limités à **5 ou 10 max**. Exécution séquentielle (un sujet par appel), arrêt au premier échec, **aucun retry automatique**, sujets réussis non rejoués, idempotence, journal sans contenu sensible. **Aucun bouton ne lance silencieusement des centaines d'appels.** | K5, K6, K7 | Terminée | Route `/illustrations/manquantes` créée (nouveau, distincte du centre de consultation `/illustrations`) ; `getMissingVisualSubjects()` (`src/lib/visuals/queue.ts`) croise `getAllVisualSubjects()` (K6) avec `getPrimaryVisualAsset` (storage.ts) — un sujet avec un visuel `approved` mais pas `isPrimary` reste dans la file (testé explicitement, pas un simple `status === "approved"`) ; total + regroupement par type + modèle/qualité/dimensions par type (`describeRealImageGenerationRequest`/`SIZE_BY_RATIO`, F-IA2) + coût (`costPerImageEstimateEur` toujours `null` aujourd'hui → « Estimation indisponible », jamais inventé) affichés avant toute génération. UI (`MissingQueueBrowser.tsx`) : sélection individuelle/tout sélectionner par type (plafonné)/désélection par type ou totale/recherche locale (nom + entreprise parente) ; taille de lot 5 ou 10 (radio, jamais plus, checkboxes désactivées au plafond) ; génération d'un seul sujet = bouton immédiat par ligne, sans phrase de confirmation (même patron que `generateAction` de `/visuels`) ; lot de 2+ = phrase de confirmation nommée obligatoire (`GENERER LOT {n}`, même patron que `BATCH_CONFIRMATION_PREFIX` E3/E4). Moteur `runVisualGenerationQueue` (généralisation de la logique de l'ancien pilote lot H, supprimée en K2 mais reprise ici) : séquentiel, arrêt immédiat au premier échec (sujets restants `not_attempted`), aucun retry automatique, sujet déjà pourvu `skipped` sans appel (revérifié juste avant CHAQUE appel, pas seulement au chargement), lot > `MAX_QUEUE_BATCH_SIZE` (10) refusé (`QueueBatchSizeError`), journal (`onEntry`) sans contenu sensible (type + id + statut + horodatage uniquement, jamais le prompt ni le libellé). **Exécution réelle dans ce lot = mode démonstration uniquement** (`generateVisualDraft`, gratuit, aucun réseau, aucune clé) : le moteur est générique (`generate` injecté), brancher `generateRealVisualDraft` (F-IA2, déjà écrit) derrière un vrai écran de confirmation de coût revient à K9 sans réécrire ce fichier — **aucun appel OpenAI réel possible dans ce lot**, conforme à la contrainte. Bug réel trouvé et corrigé en cours de tâche (même classe que K5) : `queue.ts` importe `storage.ts`/`subjects.ts` (chaîne `next/headers`), donc pas importable depuis `MissingQueueBrowser.tsx` (Client Component) — extrait dans `queueConstants.ts` (constantes/types purs, sans dépendance `src/lib/data/*`/`storage.ts`), `next build` vert après correction. Lien K5 (`/illustrations` → `/visuels?manquants=1`) mis à jour vers `/illustrations/manquantes` ; `/visuels?manquants=1` volontairement laissé fonctionnel et inchangé. `next build` vert (`ƒ /illustrations/manquantes` confirmée) ; typecheck + lint (repo entier) + 502 tests unitaires verts (dont 9 nouveaux `queue.test.ts` : file dynamique avec/sans visuel principal, approuvé-non-principal ne sort pas de la file, brouillon seul ne sort pas de la file, ordre séquentiel, arrêt au 1er échec sans retry, idempotence/skip, plafond de lot, journal sans contenu). Aucune génération réelle déclenchée en dev/test. **K9 débloquée** (dépendance K8 remplie) : reste à faire — écran de confirmation de coût réel avant tout appel payant (fournisseur/modèle/qualité/dimensions/nb d'appels/coût déjà calculés ici, réutilisables tels quels), prompt repliable, exclusions, persistance des octets réels (K10), approbation/versions (K11), intégration pages métier (K12) | `src/app/(app)/illustrations/manquantes/page.tsx` (nouveau), `src/app/(app)/illustrations/manquantes/MissingQueueBrowser.tsx` (nouveau), `src/app/(app)/illustrations/manquantes/actions.ts` (nouveau), `src/app/(app)/illustrations/manquantes/loading.tsx` (nouveau), `src/lib/visuals/queue.ts` (nouveau, serveur uniquement), `src/lib/visuals/queueConstants.ts` (nouveau, client-safe), `src/lib/visuals/queue.test.ts` (nouveau) ; lien mis à jour sans changement de comportement : `src/app/(app)/illustrations/IllustrationsBrowser.tsx`, `src/app/(app)/illustrations/page.test.tsx` |
| K9 | **Confirmation avant génération (K9)**. Avant chaque génération (individuelle ou groupée) : opération, sujets exacts + type, fournisseur, modèle, qualité, dimensions, nb images, nb d'appels, coût estimé, prompt final repliable, exclusions, confirmation explicite. Coût jamais inventé : si prix courant non fiable → « Estimation indisponible » + lien documentaire configuré. Réutilise `aiCostGuard.ts`. | K8 | Terminée | Écart réel trouvé (non conforme au contrat, confirmé en lisant le code livré par K8) : `generateSingleMissingAction` lançait la génération d'un sujet unique **immédiatement au clic**, sans aucune phrase de confirmation, contrairement au lot (≥2) qui en exigeait déjà une — corrigé en supprimant cette action et en unifiant : un sujet unique est désormais traité comme un lot de taille 1, passant par le même formulaire de confirmation (`runMissingQueueAction`, `aiCostGuard.ts` déjà consommé par cette action, inchangé) que tout lot ; cliquer « Générer ce sujet… » sur une ligne ne fait plus que présélectionner ce seul sujet et faire défiler jusqu'à l'écran de confirmation (`selectOnlyAndConfirm`, `MissingQueueBrowser.tsx`) — testé (`scrollIntoView` gardé par optional chaining, absent de jsdom). Écran de confirmation étendu pour afficher, pour 1 sujet **et** pour un lot : opération (libellé + preset « Botanique éditorial — v1 ») ; liste explicite des sujets sélectionnés avec leur type (jamais un simple total) ; fournisseur/modèle/qualité (déjà là depuis K8) ; dimensions par type réellement sélectionné ; nombre d'images ET nombre d'appels affichés distinctement (toujours égaux ici, un appel = une image, F-IA2) ; coût — `costPerImageEstimateEur` toujours `null` aujourd'hui ⇒ « Estimation indisponible » **jamais un chiffre inventé**, accompagné d'un lien vers le barème tarifaire officiel du fournisseur (nouvelle constante `OPENAI_PRICING_DOC_URL`, `openai-provider.ts` — n'existait nulle part avant cette tâche, le renvoi du prompt initial vers un lien « déjà configuré dans `aiCostGuard.ts` » était inexact, `aiCostGuard.ts` ne portant aucun lien documentaire : c'est un garde-fou d'idempotence/délai, pas une source de prix) ; un `<details>` **par sujet sélectionné** contenant le prompt EXACT construit par `buildVisualPrompt` (jamais un exemple générique) ; les exclusions du preset (`PRESET_EXCLUSIONS`) listées une fois ; confirmation explicite par phrase nommée (« GENERER LOT {n} », y compris pour n = 1, réutilise `QUEUE_CONFIRMATION_PREFIX`/le patron E3/E4/K8 sans nouvelle constante) et bouton nommant l'action (« Générer ce sujet » / « Générer le lot (n) », jamais un « OK » générique). Écart annexe trouvé en construisant le prompt affiché : `generateVisualDraft` (`service.ts`) et `generateRealVisualDraft` (`real-generation.ts`, F-IA2, prêt pour K10) n'acceptaient/ne transmettaient pas encore les 3 champs K6/K7 (`preparationNames`/`validatedKeyIngredientNames`/`additionalInformation`) à `buildVisualPrompt` — signalé mais non traité par K7/K8 — comblé (additif, rétrocompatible) dans les deux fichiers ET dans `runMissingQueueAction` (actions.ts) pour que le prompt réellement envoyé au fournisseur (démo aujourd'hui, réel via K10) soit **identique** à celui affiché à l'écran de confirmation, jamais une approximation. Garde de coût (`aiCostGuard.ts`, `beginAiRequest`/`completeAiRequest`) toujours consommée avant exécution (inchangée depuis K8, revérifiée). Aucun appel OpenAI réel déclenché. Typecheck + lint + `next build` verts ; 511 tests unitaires verts (dont 6 nouveaux `MissingQueueBrowser.test.tsx` : aucune confirmation tant qu'aucun sujet sélectionné, écran complet pour 1 sujet sans déclenchement immédiat, prompt repliable fidèle aux préparations/ingrédients validés/information complémentaire réels d'une recette, coût `null` ⇒ « Estimation indisponible » + lien, coût connu ⇒ montant calculé, lot ≥2 ⇒ phrase adaptée au nombre). **Prêt pour K10** : le moteur de file (`queue.ts`, K8) reste inchangé et générique, l'écran de confirmation ci-dessus est celui qui doit précéder tout futur appel réel ; `generateRealVisualDraft` (F-IA2) est prêt à recevoir les mêmes champs de prompt mais n'est PAS branché par cette tâche — reste à faire par K10 : persistance immédiate après réponse OpenAI, puis brancher le générateur réel dans `runMissingQueueAction`/`queue.ts` derrière cet écran. | `src/app/(app)/illustrations/manquantes/MissingQueueBrowser.tsx`, `src/app/(app)/illustrations/manquantes/MissingQueueBrowser.test.tsx` (nouveau), `src/app/(app)/illustrations/manquantes/actions.ts` (suppression `generateSingleMissingAction`, unification via `runMissingQueueAction`), `src/app/(app)/illustrations/manquantes/page.tsx` (transmission des champs K6/K7 + lien de coût), `src/lib/ai/visuals/service.ts`, `src/lib/ai/visuals/real-generation.ts` (3 champs de prompt additionnels, rétrocompatibles), `src/lib/ai/visuals/openai-provider.ts` (nouvelle constante `OPENAI_PRICING_DOC_URL`) |
| K10 | **Persistance immédiate des images (K10)**. Après réponse OpenAI réussie : valider le fichier, stocker immédiatement dans bucket privé `visual-assets`, enregistrer les métadonnées, créer statut `draft`, n'afficher le résultat qu'après confirmation de la persistance. Échec stockage/base → ne pas annoncer un succès, nettoyage sûr sinon enregistrement de l'orphelin ; ne jamais perdre silencieusement une image payante. | K-S1, K9 | Terminée | Nouveau mécanisme UNIQUE `persistGeneratedVisual` (`storage.ts`), consommé à la fois par `service.ts` (démo) et `real-generation.ts` (réel) — remplace l'appel direct à `createDraftVisualAsset` avec l'`imageUrl` brute du fournisseur dans les deux fichiers, aucune duplication. Séquence imposée et respectée : 1) `decodeGeneratedImage` valide que la réponse est une data URI bien formée, de type `image/*`, décodée non vide (`VisualGenerationValidationError` sinon, aucun octet écrit nulle part) ; 2) upload réel dans `visual-assets` (`supabase.storage`) UNIQUEMENT si Supabase est configuré ET que le MIME déclaré fait partie des formats réellement acceptés par le bucket (PNG/WebP, `20260814090300_storage_buckets.sql`, vérifié — pas de nouvelle migration nécessaire) ; 3) `createDraftVisualAsset` (inchangé) enregistre les métadonnées, `image_url` = chemin Storage réel (jamais la data URI) ; 4) statut `draft` toujours (comportement déjà garanti par `createDraftVisualAsset`, non modifié). Le visuel n'est retourné à l'appelant — donc jamais affiché comme un succès — qu'une fois les 4 étapes réellement confirmées : toute exception interrompt la fonction avant tout retour. Cas non éligible à l'upload Storage (Supabase non configuré — dev local/tests — OU mode démonstration qui produit un SVG, format que le bucket refuse, contenu gratuit jamais payant) : comportement inchangé, la data URI continue d'être écrite directement dans la colonne texte `image_url`, aucune régression sur `/visuels`/`/illustrations/manquantes` (K8/K9, tous deux encore en mode démonstration dans ce lot). Échec après stockage réussi (atteignable uniquement sur le chemin réel `real-generation.ts`, seul consommateur payant) : nettoyage immédiat tenté (`storage.remove`), incident TOUJOURS journalisé (`console.warn` structuré, bucket/chemin/sujet/message, jamais le prompt ni les octets) qu'il réussisse (« nettoyé ») ou échoue (« ÉCHEC DU NETTOYAGE, orphelin potentiellement payant, à vérifier manuellement ») — jamais de perte silencieuse. `real-generation.ts` classe désormais aussi les échecs de validation/persistance dans son résultat `err(...)` (message actionnable, jamais un « voir journal serveur » générique quand la cause est connue et sûre à afficher). **`generateRealVisualDraft` reste non branché dans la queue réelle de production** (`queue.ts`/`MissingQueueBrowser`, K8/K9 inchangés) : aucun appel OpenAI réel possible dans ce lot, conforme à la contrainte. Aucun octet/clé côté client (upload exécuté uniquement dans un contexte serveur via `createSupabaseServerClient`, jamais transmis au navigateur). RLS déjà en place (K-S1) non modifiée, non affaiblie. Typecheck + lint + `next build` verts ; 519 tests unitaires verts (dont 8 nouveaux `storage.persist.test.ts` : rejet data URI malformée/vide/type non-image avant tout appel Storage, ordre Storage→base respecté avec chemin Storage retourné au lieu de la data URI, format démo SVG jamais envoyé au bucket même Supabase configuré, échec Storage seul sans écriture base ni nettoyage, échec base seul avec nettoyage réussi + journalisation, échec des deux avec journalisation « orphelin » explicite) ; aucun appel OpenAI réel déclenché (tous les tests mockent `@/lib/supabase/env`/`@/lib/supabase/server`, aucune variable Supabase réelle, aucun `fetch` réseau). **Reste pour K11** : `approvedVisual.ts` ne sait pas encore lire un chemin Storage (`!imageUrl.startsWith("data:")` → URL signée) — sans impact ici car K10 ne crée que des brouillons, jamais un `approved`/`isPrimary`, donc `getApprovedVisualUrl` ne peut structurellement pas encore rencontrer un chemin Storage réel ; K11 (approbation) devra implémenter cette branche AVANT d'approuver un premier visuel réel, sous peine de casser l'affichage d'un futur visuel réel approuvé. | `src/lib/visuals/storage.ts`, `src/lib/visuals/storage.persist.test.ts` (nouveau), `src/lib/ai/visuals/service.ts`, `src/lib/ai/visuals/real-generation.ts` |
| K11 | **Approbation et versions (K11)**. Par brouillon : « Approuver et utiliser », « Conserver en brouillon », « Rejeter », « Générer une nouvelle version » (nouvelle confirmation de coût). Aucun brouillon utilisé dans les pages métier ; aucun brouillon approuvé automatiquement ; un seul `isPrimary` par sujet ; bascule atomique à l'approbation ; ancien visuel approuvé conservé dans l'historique ; aucune suppression automatique ; rejet sans effet sur le principal existant. **Point d'attention hérité de K10** : `src/lib/visuals/approvedVisual.ts` doit gagner la lecture d'un chemin Storage réel (URL signée temporaire) avant qu'un premier visuel réel (non-démo) ne soit approuvé, sinon son affichage casserait silencieusement. | K10 | Terminée | Correctif prioritaire livré en premier : `approvedVisual.ts` détecte désormais `!imageUrl.startsWith("data:")` et génère une URL signée temporaire (`supabase.storage.from("visual-assets").createSignedUrl(path, 3600)`) au lieu de retourner un chemin Storage brut inexploitable par un `<img>` ; une data URI (démo, ou Supabase non configuré) reste inchangée ; échec de signature → `VisualAssetPersistenceError` explicite (déjà catché par `ApprovedVisual.tsx`, comportement de repli placeholder inchangé pour cet appelant, propagation inchangée pour les autres) — 3 tests nouveaux `approvedVisual.signedUrl.test.ts` (Supabase mocké : URL signée générée pour un chemin réel, échec de signature explicite, data URI jamais passée à `createSignedUrl`). Atomicité `isPrimary` : **déjà livrée par E1/G3** (`storage.ts` — `approveVisualAsset`/`setPrimaryVisualAsset`/`rejectVisualAsset`, invariant garanti en dernier ressort par l'index unique PostgreSQL `visual_assets_one_primary_per_subject`), vérifiée et confirmée conforme (aucune régression, aucune réécriture) : bascule en deux étapes ordonnées côté Supabase (désactive l'ancien principal puis active le nouveau, jamais l'inverse — jamais deux principaux simultanés, fenêtre transitoire sans principal documentée `ponytail`, RPC dédiée hors périmètre de cet agent qui ne possède pas les migrations) ; test dédié préexistant confirmé (`storage.test.ts`, « parcours de versionnement (G3) ») : deux visuels concurrents pour le même sujet, approbation du second + bascule → un seul `isPrimary`, ancien conservé `approved`/non-principal, jamais supprimé ; rejet d'un brouillon coexistant confirmé neutre pour le principal existant (test préexistant). « Approuver et utiliser » = réutilisation directe et sans duplication de `approveAsPrimaryAction` (existant, `/visuels/actions.ts`, composait déjà `approveVisualAsset` + `setPrimaryVisualAsset`) ; « Rejeter » = `rejectAction` (existant) ; ajout d'un bonus non demandé mais gratuit par réutilisation : « Définir comme principal » (`setPrimaryAction`, existant) pour un second visuel déjà approuvé mais non principal. Ces trois actions + leurs `revalidatePath` étendus (additif) pour aussi invalider `/illustrations` et `/illustrations/manquantes` (`revalidatePublicPages`, `/visuels/actions.ts`) — plutôt que dupliquées dans un nouveau fichier, conformément à la consigne « généralise plutôt que duplique ». Intégrées dans `/illustrations` (`IllustrationEntryCard.tsx`, K5) : chaque version `draft` affiche « Approuver et utiliser »/« Rejeter » avec l'`assetId` de cette version précise ; chaque version `approved` non principale affiche « Définir comme principal » ; **« Conserver en brouillon » n'est pas un bouton** (un bouton sans effet à cliquer serait trompeur) mais une phrase explicite à côté des deux actions : ne rien approuver/rejeter EST la façon de conserver un brouillon, un brouillon n'étant jamais approuvé automatiquement — décision documentée dans le code, à reconsidérer si un besoin réel de marquage « vu »/« reporté » apparaît. « Générer une nouvelle version » : nouveau fichier `regenerateActions.ts` (Server Action `regenerateVersionAction`, un sujet = un lot de taille 1, même garde de coût `aiCostGuard.ts` et même phrase nommée `QUEUE_CONFIRMATION_PREFIX 1` que K9) + `RegenerateVersionForm.tsx` (écran de confirmation compact, mêmes champs que `MissingQueueBrowser` : fournisseur/modèle/qualité/dimensions/coût **jamais inventé** avec lien `OPENAI_PRICING_DOC_URL`/prompt EXACT repliable via `buildVisualPrompt`/exclusions du preset) — **écart réel par rapport au réutilise-`MissingQueueBrowser`-tel-quel envisagé initialement** : `getMissingVisualSubjects()` (K8) exclut structurellement tout sujet ayant déjà un visuel principal approuvé, donc la file des manquants ne peut jamais contenir un sujet pour lequel on veut justement générer une NOUVELLE version ; réutilisation faite au niveau du mécanisme (constantes, `aiCostGuard`, `generateVisualDraft`, convention de phrase, style de l'écran) plutôt que du composant React lui-même, qui n'aurait pas eu de sens dans ce contexte à un seul sujet déjà illustré. Un seul contrôle « Générer une nouvelle version… » par SUJET (pas par version, une nouvelle version s'ajoute au sujet). Un sujet sans aucune version (`missing`) n'affiche aucune action (renvoie vers `/illustrations/manquantes`, K8, inchangé). Typecheck + lint + `next build` verts ; 535 tests unitaires verts (+16 nouveaux : 3 `approvedVisual.signedUrl.test.ts`, 5 `IllustrationEntryCard.test.tsx`, 4 `regenerateActions.test.ts`, 4 `RegenerateVersionForm.test.tsx`) ; aucun appel OpenAI réel déclenché (tous les tests mockent Supabase/`generateVisualDraft`/`next/cache`). | `src/lib/visuals/approvedVisual.ts`, `src/lib/visuals/approvedVisual.signedUrl.test.ts` (nouveau), `src/app/(app)/visuels/actions.ts` (revalidation étendue, additive), `src/app/(app)/illustrations/IllustrationEntryCard.tsx`, `src/app/(app)/illustrations/IllustrationEntryCard.test.tsx` (nouveau), `src/app/(app)/illustrations/IllustrationsBrowser.tsx`, `src/app/(app)/illustrations/page.tsx`, `src/app/(app)/illustrations/page.test.tsx`, `src/app/(app)/illustrations/regenerateActions.ts` (nouveau), `src/app/(app)/illustrations/regenerateActions.test.ts` (nouveau), `src/app/(app)/illustrations/RegenerateVersionForm.tsx` (nouveau), `src/app/(app)/illustrations/RegenerateVersionForm.test.tsx` (nouveau) |
| K12 | **Intégration des visuels dans les pages métier (K12)**. Afficher le visuel approuvé principal dans : carte + page matière première, carte + fiche recette, carte + page entreprise, carte + en-tête catégorie. URLs privées/temporaires renouvelées proprement (consomme le mécanisme J5/`getApprovedVisualUrl`) ; `object-fit: contain` pour matières premières, cadrage éditorial pour recettes/ambiances ; placeholder botanique si aucun visuel approuvé ; aucun brouillon visible ; aucun saut de mise en page ; texte alternatif pertinent. Action secondaire discrète réservée aux utilisateurs autorisés : « Créer une illustration » si aucune image, « Nouvelle version » sinon — menant **toujours à l'écran de confirmation, jamais directement à l'API**. | K11 | Terminée | Inventaire : les 4 cartes (`RecipeCard`/`CategoryCard`/`SourceCard`/`CanonicalIngredientCard`) et les fiches recette/matière première consommaient déjà `getApprovedVisualUrl`/`ApprovedVisual` (J5-J7) — seuls deux écarts réels trouvés côté « carte » : `CanonicalIngredientCard` utilisait `object-cover` (contraire à la règle « matière première jamais recadrée ») et `CategoryCard`/`SourceCard`/`CanonicalIngredientCard` posaient `alt=""` même quand un vrai visuel était affiché — corrigés (fit `contain` + `alt={name}` réel sur visuel présent, `alt=""` conservé sur placeholder). Écarts réels côté page/en-tête (le cœur du travail restant, conforme au signalement du prompt) : **page entreprise** et **en-tête catégorie** n'avaient AUCUN portrait de sujet (juste un titre texte) — ajouté (`ApprovedVisual subjectType="source"/"sourceCategory"`, même patron 1:1 que la fiche matière première) ; **fiche recette** cachait le bloc visuel entier (placeholder inclus) derrière `detail.recipe.photoUrl` — un sujet illustré par IA sans photo source ne montrait donc RIEN, contraire au contrat K12 (« placeholder si aucun visuel approuvé » implique un cadre toujours présent) — `photoUrl` ne conditionne plus l'affichage (il ne sert qu'à choisir le mode de génération photo/description, `subjects.ts`), le visuel est désormais toujours rendu comme sur la carte/la fiche matière première ; test recette CAP mis à jour en conséquence (image de repli désormais attendue, alt="" vérifié). Action secondaire : nouveau composant partagé `IllustrationAction` (jamais dans les cartes elles-mêmes — imbriquer un second `<Link>` dans la carte-lien entière aurait produit une ancre imbriquée invalide — donc posée uniquement sur les 4 pages/fiches de détail) ; « utilisateurs autorisés » = mécanisme K13 réutilisé tel quel (aucune notion de rôle dans le MVP, protection héritée du shell authentifié `src/proxy.ts`, aucune vérification supplémentaire). Cible toujours un écran de confirmation, jamais un appel direct : sujet manquant → `/illustrations/manquantes?q=<nom>` (K9, jamais de génération immédiate) ; sujet déjà pourvu → `/illustrations?type=<sujet>&q=<nom>` (filtre déjà existant du centre K5, chaque `IllustrationEntryCard` correspondante expose « Générer une nouvelle version », K11/`RegenerateVersionForm`). Deep-link approché avec les mécanismes déjà existants uniquement : `/illustrations?q=` filtrait déjà par nom (K5, inchangé) ; `/illustrations/manquantes` n'avait qu'une recherche locale non pilotée par l'URL — complété a minima (`initialQuery` optionnel sur `MissingQueueBrowser`, lu depuis `?q=` par la page) pour pré-remplir cette même recherche existante, jamais une présélection ni une confirmation automatique (l'utilisateur choisit toujours explicitement « Générer ce sujet… » puis tape la phrase de confirmation). Lien K13 remplacé dans `RecipeSheet.tsx` par `IllustrationAction` (le libellé générique de repli disparaît, remplacé par le comportement complet ci-dessus). Vérifié en confiance sans modification : `getApprovedVisualUrl`/`approvedVisual.ts` (URL signée K11), `ApprovedVisual.tsx` (repli placeholder, `contain`/`cover` par type, dimensions réservées via `CulinaryFrame`/`aspect-*`, aucun saut de mise en page), invariant « aucun brouillon visible » (`getPrimaryVisualAsset` ne renvoie jamais un brouillon, revérifié par un test dédié utilisant les fixtures démo K11 : la source Hennessy n'a qu'un brouillon et s'affiche bien comme non illustrée). Typecheck + lint + `next build` verts ; 544 tests unitaires verts (+9 nouveaux : `IllustrationAction.test.tsx`, 1 `MissingQueueBrowser.test.tsx` deep-link, 1 fiche matière première déjà illustrée, 1 fiche matière première non illustrée renommé, 1 page entreprise Hennessy brouillon-seul, 1 en-tête catégorie déjà illustrée) ; aucun appel OpenAI. | `src/components/cards/CanonicalIngredientCard.tsx`, `src/components/cards/CategoryCard.tsx`, `src/components/cards/SourceCard.tsx`, `src/components/ui/IllustrationAction.tsx` (nouveau), `src/components/ui/IllustrationAction.test.tsx` (nouveau), `src/app/(app)/recettes/[slug]/page.tsx`, `src/app/(app)/recettes/[slug]/page.test.tsx`, `src/app/(app)/recettes/[slug]/RecipeSheet.tsx`, `src/app/(app)/matieres-premieres/[matiere]/page.tsx`, `src/app/(app)/matieres-premieres/[matiere]/page.test.tsx`, `src/app/(app)/entreprises/[source]/page.tsx`, `src/app/(app)/entreprises/[source]/page.test.tsx`, `src/app/(app)/entreprises/[source]/[categorie]/page.tsx`, `src/app/(app)/entreprises/[source]/[categorie]/page.test.tsx`, `src/app/(app)/illustrations/manquantes/MissingQueueBrowser.tsx`, `src/app/(app)/illustrations/manquantes/MissingQueueBrowser.test.tsx`, `src/app/(app)/illustrations/manquantes/page.tsx` |
| K13 | **Flux après import (K13)**. Après enregistrement d'une recette : afficher la recette, proposer facultativement « Créer une illustration » (jamais coché automatiquement, aucune génération pendant l'import), permettre d'y revenir depuis la fiche ou `/illustrations`. Les nouveaux ingrédients canoniques validés apparaissent automatiquement dans « Illustrations manquantes ». | K8, K3-IMPORT | Terminée | Écran de succès de `ImporterWizard.tsx` (inchangé sur les 3 liens existants « Voir la fiche recette »/« Importer une autre recette »/« Retour aux recettes ») complété d'un simple paragraphe « Facultatif — Créer une illustration » : lien `<Link>` pur (aucun `<form>`, aucun état, aucun appel réseau) vers `/illustrations/manquantes` (la file dynamique livrée par K8 — pas de route de génération pour un sujet précis distincte : K9, qui ajouterait un écran de confirmation par sujet, n'est pas encore livré ; laissé pour K9/K12 sans y toucher) + un second lien vers `/illustrations`. Rien n'est coché/déclenché automatiquement ; aucun appel OpenAI, aucune génération pendant l'import — vérifié par lecture (le lien est un simple `next/link`, aucun handler). Retour depuis la fiche recette : K12 (action « Créer une illustration »/« Nouvelle version » dans les cartes/fiches) n'est pas encore livré (dépend de K11) — comblé a minima par un lien discret équivalent dans `RecipeSheet.tsx` (sous le visuel, `text-sm text-cacao/60 underline`) vers `/illustrations/manquantes`, sans construire l'intégration complète K12 (pas de logique « Nouvelle version » conditionnelle, réservée à K12). Chaîne K6→K8 revérifiée et confirmée déjà automatique sans aucune action supplémentaire : `getAllVisualSubjects()` dérive les sujets `ingredient` directement de `getCanonicalIngredients()`, `getMissingVisualSubjects()` (K8) les croise avec le visuel principal approuvé — nouveau test dédié `src/lib/visuals/queue.k13.test.ts` (3 tests : nouveau canonique visible dans `getAllVisualSubjects()`, visible dans `getMissingVisualSubjects()` sans action supplémentaire, disparaît normalement dès qu'un visuel principal lui est approuvé) confirme le comportement avec un ingrédient canonique mocké absent du jeu de démo statique (simule un enregistrement tout juste validé, sans toucher `store.ts`/`canonical-ingredients.ts`, hors périmètre de cette tâche). Fichiers K9 (`illustrations/manquantes/**`) et `storage.ts` non modifiés (lecture seule, respecté). Typecheck + lint + tests ciblés verts (`src/lib/visuals` 159 tests, dont les 3 nouveaux ; `[slug]/page.test.tsx` 8 tests inchangés verts) | `src/app/(app)/importer/ImporterWizard.tsx`, `src/app/(app)/recettes/[slug]/RecipeSheet.tsx`, `src/lib/visuals/queue.k13.test.ts` (nouveau) |
| K14 | **Nettoyage et sécurité transverse (K14)**. Vérifier : aucune route pilote accessible ; aucun composant pilote dans la nav ; aucun fournisseur démo en Preview ; aucun fallback mémoire en Preview ; aucun secret côté client ; aucune réponse Auth mise en cache ; aucun fichier source public ; aucune image privée exposée par URL permanente ; aucun prompt contenant le document complet quand quelques infos suffisent ; aucun log de recette/ingrédient privé/sortie OpenAI brute ; RLS appliquée aux nouvelles opérations. Les adaptateurs démo nécessaires aux tests restent, mais leur activation est **explicite et impossible en Preview/Production**. | K1, K2, K10, K11, K12 | Terminée | Checklist des 12 points auditée point par point (grep/lecture, aucune supposition), **11/12 déjà conformes, 1 écart réel trouvé et corrigé** : (1) aucune route pilote accessible — confirmé, seules des références en commentaire d'historique subsistent (`illustrations/page.tsx`, `real-generation.ts`, `openai-provider.ts`, `queue.ts`), aucun répertoire `pilote*` ni lien actif ; (2) `SiteHeader.tsx` ne pointe que vers les 6 routes réelles (`/`, `/entreprises`, `/recettes`, `/matieres-premieres`, `/specificites`, `/importer`), aucun lien mort ; (3) `getVisualsProvider()` (`registry.ts`) reste codé en dur sur `demoImageProvider`, `getRealVisualsProvider`/`generateRealVisualDraft` non appelés par aucune action serveur de production (seuls les tests les invoquent) — aucun risque de bascule silencieuse ; (4) les 5 fichiers `src/lib/data/*` + `supabaseSource.ts` : zéro `catch` vers la démo, repli strictement `!hasSupabaseConfig()`, toute erreur Supabase devient `DataAccessError` explicite (`throw`) ; (5) aucun secret dans un fichier `"use client"` — `process.env.OPENAI_*`/`SUPABASE_SERVICE_ROLE_KEY` confinés à des fichiers serveur, `admin.ts` a une garde `typeof window` en défense, `client.ts` n'utilise que la clé anonyme publique ; (6) `proxy.ts`/`server.ts` utilisent `getUser()` (revalidation), aucun en-tête `Cache-Control` ajouté ; (7) bucket `recipe-sources` toujours `public=false`, `sourceFileUrl` stocké est un chemin Storage brut (jamais `getPublicUrl`, zéro occurrence dans le code) ; (8) bucket `visual-assets` toujours `public=false`, `getApprovedVisualUrl` signe systématiquement les chemins Storage réels (TTL 3600 s) et ne sert jamais un brouillon (`status !== "approved" || !isPrimary` → `null`) — **point à surveiller, non bloquant** : la galerie `/visuels`/`/illustrations` affiche `asset.imageUrl` brut pour les brouillons sans signature, actuellement sans risque réel puisque `generateRealVisualDraft` n'est câblé nulle part en production (seul le mode démo, en data URI, est atteignable) — à corriger avant que K9/K10 branchent réellement un fournisseur payant sur ces galeries ; (9) `buildVisualPrompt`/`preset.ts` ne reçoivent que libellé, `preparationNames`, `validatedKeyIngredientNames` (filtrés `confirmed` uniquement dans `subjects.ts`) et `additionalInformation` — jamais le texte brut d'un document source ; (10) **écart réel trouvé et corrigé** : `src/lib/import/store.ts` journalisait `warnings: draft.warnings` en clair dans les deux `console.info` d'enregistrement (mémoire et Supabase) — ces avertissements embarquent parfois un nom d'ingrédient réel (ex. fixture « Quantité du « Beurre » illisible… ») donc une donnée métier privée en journal serveur, non conforme à la règle déjà appliquée ailleurs (K10, orphelins Storage) ; corrigé en remplaçant par `warningsCount: draft.warnings.length` (nombre seul, comportement de journalisation « modèle + version de schéma + avertissements » de `docs/05-AI_IMPORT.md` préservé sans exposer le contenu) ; aucun autre `console.*` sensible trouvé dans `src/lib/ai/**`/`src/lib/visuals/**`/`src/lib/import/**` (seuls logs restants : statut/type/id/horodatage ou métadonnées d'erreur fournisseur, `redactSecrets` confirmé testé contre une fausse clé `sk-…`) ; (11) RLS confirmée active + policies `authenticated`-only + `grant` explicite sur les 15 tables métier dont `import_batches`/`import_items`/`visual_assets` (`20260814090100_rls_policies.sql`), nouvelle colonne `import_items.source_file_hash` additive sans lacune RLS, fonction `save_import_recipe` révoquée pour `public` et accordée au seul `service_role` (`revoke all … / grant execute … to service_role`) ; (12) `vitest.config.ts`/`vitest.setup.ts` ne définissent aucune variable Supabase/test globale, aucun `process.env.VITEST`/`NODE_ENV` lu par le code de production, seul `.env.local` (ignoré par git) existe localement. Correction appliquée : `src/lib/import/store.ts` (2 occurrences) ; typecheck + lint + tests ciblés verts (`src/lib/import` 64/64, 6 fichiers). Aucune autre modification de code (audit, pas de refonte) | `docs/11-TASK_BOARD.md`, `src/lib/import/store.ts` |
| K15 | **Tests (K15)** — niveau 3 en fin de lot uniquement : typecheck, lint, tests unitaires, build, tests d'intégration Supabase live, tests Storage + RLS, Playwright 5 formats, import image/PDF/DOCX/texte, recette multi-préparations sur une seule image, multi-captures ordonnées, quantité illisible, QS, détection doublon, rollback transactionnel, visibilité immédiate de la recette importée, file des manquants, déduplication des alias, confirmation de coût, absence de retry automatique, draft non affiché, approbation atomique, ancien principal conservé, URL privée, disparition des routes pilotes, absence de scroll horizontal. **Tous les appels OpenAI mockés.** Niveau 2 ciblé après chaque tranche. | K13, K14 | Terminée | Rapport pass/fail complet (24 points) : typecheck/lint/build/544 tests unitaires/130 tests Playwright (5 profils) tous verts ; Storage+RLS vérifiés statiquement (migrations) ; toutes les règles métier du lot (doublons, rollback, dédup alias, coût jamais inventé, aucun retry auto, draft jamais affiché, approbation atomique, URL signée, routes pilotes disparues, aucun scroll horizontal) confirmées par test dédié existant. **Non-exécutable, documenté sans invention** : tests d'intégration Supabase live et checkpoint Preview réelle (`preview-checkpoint.spec.ts`), faute d'environnement Preview/identifiants `QA_TEST_EMAIL`/`QA_TEST_PASSWORD` accessibles depuis cette session — limitation à porter au rapport K16, aucune régression fonctionnelle détectée par ailleurs. Un défaut trouvé et corrigé : `tests/e2e/responsive.spec.ts` cherchait le bouton "Importer" hors du menu burger mobile (obsolète depuis le lot J2) — corrigé pour l'ouvrir d'abord sur les profils mobiles, même patron que `preview-checkpoint.spec.ts`. **Gap opérationnel signalé pour K16** : `.env.local` portant des identifiants Supabase réels (K-S1) impose désormais l'authentification même en local, ce qui bloque `npm run test:e2e`/`test:full` sans `.env.test` dédié — à trancher en K16. | suites de tests, `tests/e2e/**`, tests colocalisés |
| K16 | **Déploiement et livraison (K16)** — commits séparés (1 lectures Supabase, 2 suppression pilotes, 3 import réel, 4 centre Illustrations, 5 file des manquants, 6 génération multi-sujets, 7 intégration visuels, 8 tests+doc). Déployer **uniquement** une nouvelle Preview depuis `develop`, aucune modif en Production. Rapport final K16 (voir liste des livrables ci-dessous), GO/NO-GO pour un premier import réel, proposition précise du premier lot d'illustrations avec sujets + coût estimé. **BLOQUANT — validation humaine requise** pour le premier import réel puis le premier lot d'illustrations. | K15 | Terminée | Preview `develop` confirmée joignable via Vercel CLI (`https://pastry-book-git-develop-thomasgzls-projects.vercel.app`, déploiement automatique sur push, target=preview, jamais production) ; aucune modification Production ; rapport K16 livré en conversation avec l'utilisateur. **Constat non anticipé, vérifié en lecture seule sur la vraie base Supabase de Preview** : `sources`/`source_categories`/`recipes`/`canonical_ingredients` = 0 ligne (base vierge de toute donnée métier réelle, aucun import réel n'a eu lieu, conforme au lot K) ; `visual_assets` contient 7 lignes historiques, toutes `subject_type=ingredient`, `subject_id` à motif fixe `00000000-0000-4000-8000-000000000201`/`202` (identifiants déterministes de l'ancien pilote Citron/Pistache, lot H, horodatage 2026-08-14), dont 2 `approved`+`is_primary` — **orphelines** : elles ne référencent aucune ligne réelle dans `canonical_ingredients` (table vide), donc invisibles dans toute page métier réelle aujourd'hui (K6 dérive les sujets `ingredient` strictement depuis `getCanonicalIngredients()`). Aucune suppression exécutée (K2/K14 : jamais sans confirmation explicite) — présentées à l'utilisateur pour décision dans le rapport K16. Premier import réel et premier lot d'illustrations **non lancés**, en attente de validation utilisateur. | commits `develop` (a0207bd…2d49551), tableau de bord Vercel (Preview) |

**Livrables du rapport final K16 :** routes pilotes supprimées ; nouveau parcours d'import ; formats acceptés ; comportement multi-préparations ; données enregistrées ; pages alimentées par Supabase ; centre Illustrations ; types de sujets compatibles ; liste et nombre des ingrédients sans illustration ; mécanisme de lot ; protections de coût ; tests et résultats ; captures téléphone/tablette/ordinateur ; URL Preview ; limites restantes ; GO/NO-GO pour un premier import réel ; proposition précise du premier lot d'illustrations avec sujets et coût estimé. **S'arrêter après la Preview.**

**Anti-chevauchement Lot K :** K-S1 (migrations/`config.toml`, propriété exclusive `data-security-agent`) se livre **en premier** ; territoires ensuite disjoints → parallélisme cadré :

- **Tranche 1 (après K-S1)** : K1 (`recipe-search-agent`, `src/lib/data/*`) et K2 (`frontend-design-agent`, routes `visuels/pilote*` + `importer/pilot*`) touchent des fichiers disjoints → **parallèle**. Note : K1 et K6 lisent tous deux `canonical-ingredients.ts` ; K6 (dérivation de sujets, fichier `src/lib/visuals/subjects.ts` distinct) démarre **après** K1 pour ne pas se disputer `src/lib/data/canonical-ingredients.ts`.
- **Tranche 2 (import, après tranche 1)** : K3-DATA (`data-security-agent`, `src/lib/import/store.ts` + persistance) **précède** K3-IMPORT (`ai-import-agent`, `src/app/(app)/importer/**`) qui **précède** K3-UI (`frontend-design-agent`, écran de correction dans `importer/steps/**`). Séquentiel car même aire `importer/**` pour IMPORT et UI → jamais deux verrous simultanés sur `importer/**`.
- **Tranche 3 (illustrations, après tranche 1)** : K5 (`frontend-design-agent`+`ai-visuals-agent`, nouvelle aire `src/app/(app)/illustrations/**`) puis K6→K7→K8→K9→K10→K11 majoritairement dans `src/lib/visuals/**` (`ai-visuals-agent`) et `src/app/(app)/illustrations/**` (UI) ; `storage.ts` est écrit par `ai-visuals-agent` en K10/K11 → à verrouiller à ces tâches. K12 (`frontend-design-agent`, cartes/pages métier) après K11. La tranche import (K3-*) et la tranche illustrations partagent `data-security-agent` (K3-DATA vs K-S1/K10 Storage) : séquencer côté `data-security-agent` pour éviter deux migrations concurrentes.
- **Verrous** posés seulement au démarrage effectif d'une tâche (`En cours`). `qa-integration-agent` intervient en niveau 2 après chaque tranche livrée, niveau 3 unique en fin de lot (K15). Le contrat gelé `src/lib/domain/schemas.ts` / `src/lib/supabase/types.ts` n'est **pas** modifié par K ; toute lacune passe par `data-security-agent` en passe unique (règle héritée des lots D/E/F-IA).

## Lot F — Validation

Propriétaire : `qa-integration-agent`

| ID | Tâche | Dépendances | Statut | Critères de validation | Fichiers concernés |
|---|---|---|---|---|---|
| F1 | Plan de tests dérivé de `docs/08-ACCEPTANCE_CRITERIA.md`, incluant matrice téléphone/tablette portrait/tablette paysage/ordinateur et checklist PWA | A4 | À faire | Chaque critère converti en test vérifiable, y compris les critères « Design et responsive » et « PWA et hors connexion » | plan de tests |
| F2 | Validation de chaque lot avant démarrage du lot dépendant suivant | correspond au lot validé | À faire | Rapport clair, défauts assignés à l'agent propriétaire | rapports de validation |

## Tâches prêtes

Lot C / C-bis terminés et validés (C-bis validé définitivement par l'utilisateur le 2026-08-14). **Lots D et E lancés en parallèle** (territoires de fichiers disjoints, verrous inscrits au ledger). Mode économie de tokens permanent (niveaux 1/2, §9 du protocole).

Tâches immédiatement `Prête` :

1. **D1** — Parcours d'import manuel fonctionnel (`ai-import-agent`). Puis **D2** (modèle + validation stricte, `Prête` en parallèle car fichiers disjoints de D1) ; **D3** débloquée après D1+D2 ; **D1b** (frontend) débloquée après création de `/importer`.
2. **E2** — Preset « Botanique éditorial — v1 » (`ai-visuals-agent`). Puis **E1** (infra fournisseur) après E2 ; **E3** après E1 ; **E4** après E2+E3.

**Lot F-IA (2026-08-14) — code des adaptateurs OpenAI réels, aucun appel payant.** Ordre : `data-security-agent` livre **F-IA3** (2 lignes `.env.example`) + **F-IA4** (helper garde de coût partagé) d'abord ; puis **F-IA1** (`ai-import-agent`) et **F-IA2** (`ai-visuals-agent`) en vrai parallèle (territoires disjoints). Tests mocks uniquement, aucun appel OpenAI réel, aucune vraie clé même en test. Checkpoint QA unique final par `qa-integration-agent`. La clé sera ajoutée manuellement par l'utilisateur dans `.env.local` après ce lot — ne jamais la demander en conversation.

**Lot I (2026-08-14) — persistance Supabase réelle + Preview Vercel.** Audit I1 terminé. Ordre : `data-security-agent` livre **I2** (migration corrective `source_file_url` nullable + buckets Storage) d'abord ; puis **I3 BLOQUANT — action humaine requise** (l'utilisateur exécute `supabase login`/`link`/`db push` et remplit `.env.local`) ; ensuite **I4** (visual_assets persistant), **I5** (import persistant, après I4), **I6** (upload média) et **I7** (lecture avec repli démo, parallèle) ; puis **I8 BLOQUANT — action humaine requise** (connexion Vercel + Preview) ; **I9** checkpoint QA niveau 3 sur la Preview ; **I10** documentation (recréation `.env.example` sans secret). Ne jamais lire ni committer `.env.local`. Ne jamais affirmer que Supabase est fonctionnel avant confirmation utilisateur (I3).

**Lot J (2026-08-14) — finition visuelle complète de la Preview.** Audit J0 terminé (captures 5 gabarits, utilisateur de test jetable créé puis supprimé, 0 restant). Ordre : `frontend-design-agent` livre **J1** (tokens, fondation) en premier ; `data-security-agent` livre **J5** (lecture Storage sécurisée) en parallèle **une fois I4/I6 réellement clôturés** ; puis **J2** (header), **J4** (cadre/ornement, corrige le débordement P1) après J1 ; **J3** (accueil) après J1+J4 ; **J6** (fiche), **J7** (cartes/matière), **J8** (galerie) après J1+J4+J5 ; **J9** (rebranchement pilotes, `ai-visuals-agent`, aucun appel réel) après J5, parallèle à J8 ; **J10** passe responsive/accessibilité transverse ; **J11** checkpoint QA niveau 3 sur la Preview. Aucune génération/extraction IA payante, aucune modification de contrat/migration/variable Vercel, aucun changement de logique métier. D1b est **fusionnée dans J2**.

Tâche immédiatement `Prête` : **J1** (consolidation des tokens, `frontend-design-agent`).

**Lot K (2026-08-15) — imports réels, suppression des pilotes, illustrations complètes.** Inventaire K0 terminé (routes pilotes réelles listées, repli démo confirmé dans `src/lib/data/*`, `/illustrations` inexistant, preset v1 déjà versionné). Réconciliation : I4–I10 et J1–J12 considérés `Terminée` par preuve de commits + Preview live. Ordre : `data-security-agent` livre **K-S1** (vérif schéma/RLS/buckets `recipe-sources`+`visual-assets`) en premier ; puis **K1** (`recipe-search-agent`, lectures Supabase sans repli silencieux) et **K2** (`frontend-design-agent`, nettoyage pilote) en parallèle (territoires disjoints) ; ensuite tranche import **K3-DATA → K3-IMPORT → K3-UI** (séquentiel, aire `importer/**`) + **K4** doublons ; en parallèle tranche illustrations **K5** (centre `/illustrations`) → **K6 → K7 → K8 → K9 → K10 → K11** (sujets/file/confirmation/persistance/approbation) → **K12** (intégration pages métier) → **K13** (flux après import) ; puis **K14** (nettoyage/sécurité), **K15** (tests niveau 3, `qa-integration-agent`), **K16** (nouvelle Preview `develop` + rapport final). `qa-integration-agent` valide en niveau 2 après chaque tranche. **K-S1 livrée** (2026-08-15) : buckets/RLS/`source_file_url` nullable déjà en place depuis I2, seule lacune comblée = colonne `source_file_hash` (K4). **K1 livrée** (2026-08-15, `recipe-search-agent`) : `src/lib/data/*` branché sur Supabase sans repli silencieux, voir détail dans la ligne K1 ci-dessus — inclut une lacune connue et signalée (non corrigée, hors périmètre K1) : 4 fichiers `src/app/(app)/importer/**` (`ImporterWizard.tsx`, `steps/SourceStep.tsx`, `steps/RecipeDetailsStep.tsx`, `steps/ReviewStep.tsx`) appellent encore la couche data de façon synchrone et cassent le typecheck + le rendu s'ils sont ouverts ; à corriger mécaniquement (async/await) par `ai-import-agent`/`frontend-design-agent` en tout début de K3-IMPORT/K3-UI, avant toute autre modification de ces fichiers. **K2 déjà livrée** (voir ligne K2). **K6 livrée** (2026-08-15, `ai-visuals-agent`) : `src/lib/visuals/subjects.ts` dérive les sujets `ingredient` depuis le registre canonique uniquement (dédup des alias structurelle, pas un scan de libellés), sujets `recipe` enrichis de `preparationNames`/`validatedKeyIngredientNames` (ingrédients `confirmed` uniquement)/`additionalInformation` pour K9, voir détail ligne K6 — aucune génération, aucun appel OpenAI. **K5 livrée** (2026-08-15, `frontend-design-agent`) : centre `/illustrations` créé, consultation seule (aucune action de génération/approbation — celles-ci restent dans `/visuels` jusqu'à K8-K12), voir détail ligne K5 ci-dessus. K7 (dépendait de K5) et K8 (dépendait de K5+K6, les deux désormais `Terminée`) sont débloquées. **K3-DATA livrée** (2026-08-15, `data-security-agent`) : transaction atomique réelle via fonction Postgres `save_import_recipe` (RPC, `service_role` uniquement), hash SHA-256 du fichier source calculé et persisté, upload durci (taille 8 Mo + MIME réel par signature binaire), détection de doublon étendue (`checkImportDuplicates`, 3 nouveaux cas K4, non câblée à l'UI — laissée à K3-IMPORT/K3-UI), voir détail ligne K3-DATA ci-dessus ; signale à K3-IMPORT une incohérence à corriger en priorité (`FilesStep.tsx` limite 20 Mo au lieu de 8 Mo). **K7 livrée** (2026-08-15, `ai-visuals-agent`) : `VISUAL_PRESET_VERSION`/`BASE_PROMPT` inchangés ; lacunes réelles trouvées et comblées de façon additive dans `FRAMING.ingredient` (échelle petite carte, éléments secondaires tolérés seulement s'ils aident la reconnaissance, interdiction explicite d'inventer un ingrédient voisin), `FRAMING.recipe` (jamais une photo contractuelle du dessert réel) et `PRESET_EXCLUSIONS` (main, marque/packaging, vaisselle inutile, ombre lourde, cadre dessiné manquaient) ; `BuildVisualPromptInput`/`buildVisualPrompt` exploitent désormais `preparationNames`/`validatedKeyIngredientNames`/`additionalInformation` (K6) quand présents, silencieux sinon — voir détail ligne K7. **K3-IMPORT livrée** (2026-08-15, `ai-import-agent`) : correctif prioritaire `FilesStep.tsx` (limite réelle 8 Mo, plus 20 Mo) livré en premier ; aperçu/réorganisation/retrait de fichiers ajoutés (`FilesStep.tsx`) ; `checkImportDuplicates` (K3-DATA) branché de façon non bloquante à l'écran de vérification (`checkImportDuplicatesAction`, `ImporterWizard`, `ReviewStep` — bloc « Différences trouvées »), catégories confirmées strictement locales à leur entreprise, comportement multi-préparations « Riz au lait Vanille Caramel » confirmé correct via le test déjà existant `rizAuLait.fixture.test.ts`, lien direct vers la fiche recette ajouté à l'écran de succès — voir détail ligne K3-IMPORT ci-dessus. Aucune extraction OpenAI réelle. Typecheck + lint + `next build` + 490 tests unitaires verts. **K3-UI livrée** (2026-08-15, `frontend-design-agent`) : écran de correction (`RecipeDetailsStep.tsx`) déjà quasi complet vérifié en intégralité — seule lacune réelle trouvée : ordre des préparations non modifiable, comblée par réorganisation monter/descendre (même patron que `moveFile`/`FilesStep.tsx`), voir détail ligne K3-UI ci-dessus ; entreprise/catégorie confirmées volontairement non éditables directement à cet écran (fixées aux étapes 1/2, modifiables via « Précédent ») ; `QS`/`needs_review`/confirmation explicite/absence de déduction de spécificité tous confirmés déjà corrects. **Tranche 2 (import) intégralement `Terminée`** (K3-DATA/K3-IMPORT/K3-UI). **K8 livrée** (2026-08-15, `ai-visuals-agent`) : route `/illustrations/manquantes` (sélection individuelle/par type/désélection/recherche, lot plafonné 5/10, phrase de confirmation nommée pour tout lot de 2+, génération d'un seul sujet immédiate sans phrase) alimentée par `getMissingVisualSubjects()`/`runVisualGenerationQueue()` (nouveau `src/lib/visuals/queue.ts`, moteur séquentiel généralisé depuis la logique de l'ancien pilote lot H supprimé en K2 — arrêt au 1er échec, aucun retry automatique, idempotence revérifiée avant chaque appel, journal sans contenu sensible) — voir détail ligne K8. Exécution réelle dans ce lot reste en mode démonstration uniquement (`generateVisualDraft`, gratuit, aucun réseau) : le moteur est générique (`generate` injecté) pour que K9 y branche `generateRealVisualDraft` (F-IA2, déjà écrit) derrière un vrai écran de confirmation de coût sans réécriture — aucun appel OpenAI réel possible dans ce lot. Bug réel trouvé et corrigé (même classe qu'en K5) : constantes/types client-safe extraits dans `queueConstants.ts` pour que `MissingQueueBrowser.tsx` (Client Component) n'importe jamais `storage.ts`/`subjects.ts` (chaîne `next/headers`). Lien K5 mis à jour vers `/illustrations/manquantes` (`/visuels?manquants=1` conservé, inchangé). `next build` + typecheck + lint (repo entier) + 502 tests unitaires verts. **K9** débloquée, immédiatement `Prête` (`ai-visuals-agent`, écran de confirmation de coût réel dans `src/app/(app)/illustrations/**`). **K10 livrée** (2026-08-15, `ai-visuals-agent`) : mécanisme UNIQUE `persistGeneratedVisual` (`storage.ts`), consommé par `service.ts` (démo) ET `real-generation.ts` (réel), remplace l'appel direct à `createDraftVisualAsset` avec l'`imageUrl` brute — voir détail ligne K10 ci-dessus. Ordre imposé validation→Storage (si Supabase configuré et MIME accepté par le bucket, PNG/WebP)→base→`draft`→retour à l'appelant respecté ; aucun succès retourné avant persistance réellement confirmée ; échec après stockage réussi ⇒ nettoyage tenté puis incident TOUJOURS journalisé (jamais silencieux), qu'il réussisse ou non. Mode démonstration (SVG, jamais payant) et absence de Supabase (dev/tests) inchangés par construction (bucket refuse le SVG, comportement historique préservé, zéro régression sur `/visuels`/`/illustrations/manquantes`). `generateRealVisualDraft` toujours non branché dans la queue réelle de production (K8/K9 inchangés) : aucun appel OpenAI réel possible dans ce lot. Typecheck + lint + `next build` + 519 tests unitaires verts (8 nouveaux, tous mockés). **K11 débloquée, immédiatement `Prête`** (`ai-visuals-agent`, approbation/versions — devra d'abord faire lire à `approvedVisual.ts` un chemin Storage réel via URL signée avant d'approuver un premier visuel réel, signalé ligne K10/K11).

Garde-fous Lot K (en plus des garde-fous permanents) : aucune extraction OpenAI réelle, aucune génération d'image réelle, aucun import réel, aucune suppression incertaine de données, aucun déploiement Production — le lot s'arrête à une nouvelle Preview. Premier import réel puis premier lot d'illustrations : **BLOQUANT — validation humaine requise** après livraison du rapport K16. Ne pas toucher au `.env.example` supprimé non commité (antérieur à ce lot). Tous les appels OpenAI mockés en test.

Garde-fous de cette phase :

- Aucune modification des fonctionnalités validées du lot C (recherche, coefficient, fiche recette, données démo, contrats).
- Aucun appel IA payant (D et E) sans autorisation explicite de l'utilisateur ; passerelle coût obligatoire avant tout appel réel (E4), et avant le passage import manuel → import assisté (protocole §8).
- Contrats `src/lib/domain/schemas.ts` / `src/lib/supabase/types.ts` gelés : toute lacune passe par `data-security-agent` en passe unique avant reprise.
- Un seul checkpoint QA niveau 2 par lot (D et E), périmètre annoncé à l'avance.
- Hygiène serveur : un seul `next dev` actif avant toute capture, captures nommées lot + date, aucune galerie réutilisée d'une session précédente.
